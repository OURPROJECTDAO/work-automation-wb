# 2026-08-04 채널마진모니터 자사몰(카페24) 채널 추가 — 9번째 채널

## 무엇 / 왜
자사몰(카페24) 상품 목록을 채널마진모니터에 편입 + 가격변경 양식 생성까지.
`baseline_margin.csv` 에 자사몰 열은 이미 있었으나(1,230건) 실제 연동은 없던 상태.

사용자 제공: ① 카페24 상품 다운로드 `tdmart12_YYYYMMDD_*.csv`(암호 zip, 468건 88열)
② 구 1세대 VBA `오픈마켓가격바꾸기V4.xlsm` — **가격변경 방식 규명의 결정적 자료**.

## 구 VBA에서 확정한 사항 (CAFE24Module1~3 + 시트 3종)
- `(자사몰)일괄수정`(다운로드 원본) / `(자사몰)매크로파일`(바꿀 상품·가격) / `(자사몰)양식`(업로드용)
- `CAFE24_CompareAndCopy` → 매크로파일 A열(상품코드) 매칭 행 **통째로** 양식에 복사
- `JasaMall_UpdateAndColor` → **양식 T열에만 바꿀가격 기입**(빨간 글씨)
- `SMART_SaveSheetAsNewWorkbook` → `자사몰(YYYYMMDD).xlsx` 저장
- ★ **별도 업로드 양식 없음 = filter형**(append 아님). 쿠팡과 같은 계열.
- ★ **상품가(S=19)는 미기입** — T(판매가)만 바꾸면 카페24가 자동 재계산. (원 VBA가 그랬음)

## 데이터 실측 (오늘 다운로드 468건)
- `모델명`(L=12)이 **`[45-11` 형태로 여는 대괄호만 달림**(468/468, 닫는 괄호 0).
  안 벗기면 product_master 매칭 전건 실패.
- `자체 상품코드`(B)는 전건 공백 — 원래 관리코드 자리인데 미사용, 모델명에 넣어 운영 중.
- 코드해석: 박스 459 · 소분 8(BT10EA·BT15EA, sobun.csv로 해소) · **미매칭 1(`26-01`)**.
- `소비자가`(Q) 전건 0.00 → **정가 미사용**(jeong_field 없음).
- `상품가`(S) = 판매가/1.1 468/468 정확 일치(VAT 제외가). 마진은 판매가만 사용.
- 배송비 관련 열(배송비 구분·배송비입력·배송방법) **전건 공백** → ship_fee_const 필요.
- 열 수 **86(구 VBA) → 88(현재)**: 제조사명(49)·브랜드명(52) 추가. **A~T 구간 완전 동일**
  (판매가 20·모델명 12 불변) → 위치 지정 방식 안전.

## 변경 (work-automation-app, 2파일)
- `core/workflows/channel_margin_monitor.py`
  - `CHANNEL_CONFIG["자사몰"]` 신설: key `cafe24` · commission **0.033**(PG 3.3%, 판매수수료 없음)
    · ship_settle 0.967 · real_ship 2700 · **ship_fee_const 3000**(타 오픈마켓 동일)
    · baseline_col `자사몰` · apply_floor True · n_source `ref`
    · cols{상품번호 1, 코드 12, 상품명 8, 판매가 20} · header 1 · data_start 2
  - **`file_format: "csv"` 옵션 신설** + `_parse_csv_download()` — parse_download 가
    `load_workbook` 전용이라 CSV 채널이 불가능했음. xlsx 경로와 동일 스키마 반환.
  - **`code_strip_chars` 옵션 신설** + `_strip_code()` — 코드 양끝 문자 제거.
    xlsx 경로에도 동일 적용(`_nfc` → `_strip_code`)해 타 채널도 쓸 수 있게.
  - **`build_csv_filter_xlsx()` 신설** — CSV 원본 → 선택 행만 + T열 교체 → **xlsx 출력**.
    쿠팡 filter형의 zip 수술이 불필요(입력이 CSV라 보존할 네이티브 포맷이 없음) → Workbook 신규 생성.
- `app/pages/6_채널마진모니터.py`
  - `_raw_ext(cfg)` 신설 · `_raw_path(key, ext)` · `_commit_raw(key, bytes, ext)` —
    raw 저장이 `listing_{key}.xlsx` 고정이었음 → 자사몰은 `listing_cafe24.csv`.
  - 업로더 `type=[_rawext]` (자사몰=csv).
  - `native_raw` 가드에 `csv_filter` 포함 → **'신규만 추가' 비활성**
    (`append_rows_to_raw` 가 openpyxl 기반이라 CSV 에 적용 불가).
  - `price_form.mode == "csv_filter"` 분기 + 안내 문구.

## 검증 (실데이터 468건)
- 테스트1 CSV 파싱: 468/468 · **대괄호 전건 제거 확인**.
- 테스트2 run(): 총 468 · 미매칭 1 · 미설정 6 · 마진미달 42 · 제한 41 · **평균 마진율 9.89%**
  (타 채널 식봄 9.2·캐시노트 9.2·ESM 9.5·올웨이즈 9.6·배민 10.2·알리 10.1 과 동일 대역).
  합포 N 분수 정상 동작(선물세트 0.2/0.25).
- 테스트3 양식 생성(미달 6건 선택): 헤더 88열 원본 완전 일치 ·
  **T열만 권장가로 교체(숫자셀)** · **S열 상품가 원본 보존** ·
  **나머지 87열 전부 원본 보존** · preview/skipped/missing 정상. ✅ PASS
- 커밋 후 원격 재fetch 2파일 ast OK.

## ★ 발견 — 역마진 10건 중 1건은 hapo 누락이었음
`45-11` 복합1호(P0000BCK)가 마진 **−283.78%**(매입 97,500 vs 판매 24,000)로 나왔는데,
**같은 관리코드 `45-11` 의 P0000BBA(스팸 복합1호)는 N=0.2 로 매입 19,500·마진 7.80% 정상**.
→ product_master 오류가 아니라 **`hapo_multiplier.csv` 에 P0000BCK 미등록 → N=1 폴백**.
나머지 역마진 9건(일화 천연사이다 −6.47%, 게토레이 −4.74%, 진로 토닉워터 −2.82%,
코카콜라제로500 −2.39%, 도라지차 −2.30%, 햇반 −1.97%, 프렌치카페 −1.12%, 맥콜 −0.36%)은
N=1 이 맞는 일반 상품이라 **실제 가격 문제**.

## 다음 / 상태
- ⚠️ **재배포 + Reboot app 필요**(core 모듈 변경 → sys.modules 캐시).
- Reboot 후 사용자 최초 1회: **'상품관리 갱신 → 전체 교체'** 실행해야
  `listing_cafe24.csv` + raw 가 저장되고 가격변경 양식 생성이 가능해짐.
- **미해결(사용자 확인 대기 — "나중에확인함")**:
  ① `26-01` 미매칭 1건(소분표에도 없음) ② `P0000BCK` hapo 미등록
  ③ 카페24 업로드 실검증 미완 — openpyxl 생성 xlsx 를 카페24가 받는지 **실업로드 확인 필요**
  (식봄·캐시노트·배민·올웨이즈·ESM 은 동일 방식 정상, 쿠팡만 거부 전례).
- 자사몰 매출의 영업이익현황 대시보드 편입은 별개 — 이 파일은 상품 목록이라 불가.
  천년경영 ERP 에 자사몰 매출이 잡히는지 확인 필요(잡히면 이미 반영된 것).
