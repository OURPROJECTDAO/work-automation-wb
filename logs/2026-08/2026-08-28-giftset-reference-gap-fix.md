# 2026-08-28 선물세트 기준데이터 결손 보정 — sub_list 누락으로 ERP 낱개 미이동

추석26 **D-28**(관측 국면). 사용자 지적("45-10-10이 업로드될 때 낱개로 안 빠진다")에서 출발해
라인업 40종 전수 감사로 확장.

## 무엇 / 왜

`cheonnyeon_upload.process`의 전체↔낱개 분기는 **`sub_list` 멤버십이 유일 근거**다.

```python
(units[unit] if r["B"] in sub_map else keep)
```

45-10-10(Cj 스팸12호·박스내품 5·재고 89박스)은 `product_attributes`·`logistics_classification`엔
선물세트로 등록돼 있었으나 **`sub_list`·`unit_list`·`spec_master` 셋 다 없었다** → 전체 시트에 잔류 →
ERP에 세트 수량이 박스로 꽂힘.

**게다가 `detect_box_anomalies`가 못 잡는다.** 신호 A(영문자 코드)·신호 B(`[낱개` 태그) 둘 다
선물세트엔 해당 없음(코드가 숫자-하이픈, 상품명에 낱개 태그 없음) → **경고조차 안 떴다.**

## 전수 감사 결과 — 6종 결손

라인업 정본(`giftset_lineup_2026.csv` 40종) × 5파일 대조.

| 관리코드 | 상품 | 결손 |
|---|---|---|
| 45-10-10 | Cj 스팸12호 | sub_list, unit_list, spec_master |
| 45-12-05 | CJ 스팸복합레드라벨 | sub_list, unit_list, attributes, logistics |
| 50-71-05 | 동원 스페셜S호 | **5파일 전부** |
| 50-71-06 | 동원 스페셜10호 | **5파일 전부** |
| 50-75-05 | 동원 튜나리챔102호 | **5파일 전부** |
| 45-20-05 | 애경 마음14호 | spec_master |
| 83-62 | 사조 안심특선88호 | unit_list |

★ **뒤 4종은 8/27에 채널 30리스팰까지 전개된 상태였다.** 그날 `baseline_margin`·`hapo`만 커밋하고
§8-B 기준데이터 5파일을 통째로 빠뜨렸다(재고 4,905만). 채널 등록은 눈에 보이고 기준데이터는 안 보여서
조용히 빠지는 구조.

## 변경 (app repo reference, 코드 변경 0)

- `sub_list.csv` +5행 — 낱개개수 1 · 원코드 자기자신 · 구분 선물세트
- `unit_list.csv` +6행 — 박스내품 · 선물세트 · 입력값 1/박스내품 · 자기참조 (83-62 포함)
- `product_attributes.csv` +4행 · `logistics_classification.csv` +4행
- `spec_master.csv` +5행 — 45-20-05 규격 꼬리 **`*5`→`*6` 정정**(사용자 확정, 박스내품 6)
- `unit_list.csv` **PC코드 7건 개행 오염 제거**(별건, 아래)

§8-C 1단계 충돌 점검 선행 — 이 6종을 원코드로 참조하는 다른 코드 없음·`sobun` 미등록·깨끗함.

## 별건 — 참조 CSV 개행 오염 전수 스캔

2026-08-04 등재 함정(`sanitize_ref_df`)의 **픽스 이전 잔재**. 전 참조 CSV 스캔 결과 키 오염 9건:

- `unit_list` PC코드 **7건** — `'PC004305\n' != 'PC004305'` → `unit_origin_map` 미매칭 →
  박스 재고 대조·최근입고일·평균매입주기 전부 공백. **제거 완료**(312→319건).
- `logistics_classification` **2건 — 24-49-04·24-49-01(카프리썬)**. 물류 분류 조회 실패 상태.
  ⚠️ **사용자 지시로 이번엔 손대지 않음(미해결).**

무해 4건(상품명·규격 선행공백: 45-10-09 ×3·03-20)은 조회 키가 아니라 방치.
★ **선물세트 중 조회 키가 깨진 건은 0건**(45-10-09가 걸렸으나 오염 칸이 `상품명`이라 무해).

## 검증

- 커밋마다 PUT 직전 최신 sha 재취득 + 레코드 단위 대조(기존 행 무변형·행 증가 정확·열 정합).
- 재파싱: 라인업 40종 5파일 **결손 0 (40/40 완비)**.
- `입력값 == 1/박스내품` 전건 일치. ERP 낱개 분기 시뮬 6종 전부 이동 O(`J=D×1`, `K=H`).
- PC 7건: 레코드 430행 불변 · 변경 정확히 7행 · `관리코드` 외 칸 보존 · 콤마 포함 `11-74` 따옴표 행 보존.
- BOM·EOL 원본 서식 보존(`product_attributes`만 CRLF, 나머지 LF).

## 다음 / 상태

- **reference 라이브 read → Reboot·재배포 불요.** 다음 천년경영 업로드부터 반영.
- ⚠️ 미해결: `logistics_classification` 24-49-04·24-49-01 개행 오염(사용자 판단 대기).
- ⚠️ 미해결: ERP `product_master` 45-20-05 규격 `*5` 표기 정정(사용자 직접). 상품관리 재업로드 시
  앱 사본이 덮어써지므로 ERP 쪽을 고쳐야 함.
- 이월(전일): 리테일 listing 오염 복구(Reboot+전체 교체) · 쿠팡 9상품 병합 후 hapo 재커밋 · 승인요청 ·
  J호 캐시노트 4.46% 미달.
- **다음 한 수 = 진도판**(관측 국면 D-28, 8/25 이후 매출자료 미갱신). 준비물 = 영업이익현황·상품관리·ESM listing.
- 부수 확인: 등재갭 실측 결과 **ESM listing 스냅샷이 8/25로 stale**(8/27 등록 4종 미반영) —
  실질 39/40 등재로 8/12 "ESM 19/41" 경고는 종료. 자사몰만 실제 갭 6종.
