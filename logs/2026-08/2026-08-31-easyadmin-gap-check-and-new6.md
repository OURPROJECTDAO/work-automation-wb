# 2026-08-31 이지어드민 누락 점검 + 시즌 라인업 6종 등록 양식 + 양식 박제

추석26 **D-25**. 사용자 요청: "이지어드민 업로드 상황인데, 우리 운영품목 중에서 아직 이지어드민에
준비 안 된 상품 있는지 봐주라."

## 1. 누락 점검 (신규 절차)

이지어드민 `전체상품목록` export(1,307행) vs 전 채널 `listing_*.csv` 대조.

⚠️ **export는 확장자만 `.xls`이고 실제로는 UTF-8 HTML 테이블** → `xlrd` 실패
(`Expected BOF record; found b'\n<meta h'`). `pd.read_html(io.StringIO(...))` 로 읽어야 한다.

코드 추출 = `상품명` 선두 대괄호. 여러 개면(`[270301임박][15-03-03]…`) **코드 유니버스에 있는 것**을 택한다.
유니버스 = `product_master` ∪ `unit_list` ∪ `sub_list`. ★ **PC코드는 product_master에 없어서**
처음엔 326행이 추출 실패했다 → 유니버스 확장 후 1,279/1,307 성공.

★ 채널 `코드` 컬럼엔 **바코드·조합키 등 비관리코드가 섞여 있다**(1,267 중 364건: 리테일 바코드
`1000577670001`, 조합키 `11-40-001-CB-11-40-112` 등). 유니버스 교집합 없이 대조하면 **497건 오탐**이 났다.

**결과: 채널 등재 관리코드 903 중 133건이 이지어드민에 없음**(ESM 97·쿠팡 75·스스 42·캐시노트 41…).

### 🔴 시즌 라인업 6종 (즉시 처리)

| 코드 | 상품 | 등재 채널 |
|---|---|---|
| 45-12-05 | CJ 스팸복합레드라벨 3세트 | 4 (스스·ESM·쿠팡·캐시노트) |
| 45-20-05 | 애경 마음14호 6세트 | 5 (+자사몰) |
| 45-28 | CJ스팸복합100호 3세트 | 5 (+자사몰) |
| 50-71-05 | 동원 스페셜S호 3세트 | 4 |
| 50-71-06 | 동원 스페셜10호 5세트 | 4 |
| 50-75-05 | 동원 튜나리챔102호 3세트 | 4 |

★ **8/27 신규 4종이 그대로 포함.** 그날 채널 30리스팅을 전개하며 기준데이터 5파일을 빠뜨린 것과
**같은 패턴**(§8-B 체크리스트는 기준데이터만 다뤘고 이지어드민은 빠져 있었다).
45-20-05·45-28은 8/24 등록분이라 **일주일간 방치**. 정산 경유 채널이라 주문 시 매칭 불가 상태였다.

## 2. 산출 — 등록 양식

`easyadmin_giftset_new6_20260831.xls`. 선물세트 전용 규칙(§선물세트) 적용:
A=`[코드]{이름}(BOX/{박스내품}) 1세트` · B=20035 · C=E=A에서 코드 제거 · **D=A 그대로**(박스 규칙 아님) ·
AB=4 · AC=박스내품(3/6/3/3/5/3).

★ **공급처코드 규칙 갱신** — KB는 "브랜드/시기별로 다르니 같은 계열 최근 건을 보라"였는데,
export 65건 전수 확인 결과 **시기로 갈린다**: **2025-09 이후 등록분은 브랜드 무관 전건 20035(백설)**
(CJ뿐 아니라 사조 83-62·청정원 01-17/01-19-06도 20035). 20003(선물셋트)은 2025-01 이전 구 등록분 27건.
→ **현행 신규는 20035 고정**으로 단순화.

## 3. 양식 박제 (숙원 해소)

`상품_일괄등록_샘플_v10.xls`는 Drive·app repo 어디에도 없어 **매 배치 사용자에게 재요청**하던 상태
(2026-06-09·07-02·07-28 로그에 반복 기록).
→ `work-automation-app/reference/easyadmin_bulk_template.xls` 로 **원본 바이트 그대로 박제**
(26,624 bytes, md5 `a0c583646ab32c696605770b8e10ad74`). 다음 배치부터 raw 다운로드해서 바로 클론.

## 검증

- 양식: 69열·시트 `Sheet1`·헤더 1행 — KB 기록과 일치. 기입 열은 **헤더명으로 조회**(하드코딩 금지),
  AB=27·AC=28 재확인.
- 산출 `.xls`는 `xlrd(formatting_info=True)` → `xlutils.copy` → `ws.write` → `.xls` 저장
  (**openpyxl 재생성 금지** — "헤더가 맞지 않습니다" 원인).
- 재로드 대조: 7행 x 69열 · 헤더 무변형 · 각 행 **기입 열이 정확히 {0,1,2,3,4,27,28}** ·
  나머지 62열 공백 · D=A · C=E=A에서 코드 제거.
- 박제: 원격 raw 바이트가 원본과 **완전 동일**(md5 일치), xlrd 재로드 정상.

## 다음 / 상태

- 업로드 후 **`erp관리코드` 수동 기입** 필요(양식 69열에 없음). export 주면 상품명 대괄호에서 추출해 채움.
  ⚠️ 수동이라 오배정 전례 있음(리치스파인애플 02169) → 등록 직후 대조 검증할 것.
- ⚠️ 미착수: **라인업 외 127건** — 판매중지 잔존 리스팅 가능성이 커서 최근 주문 이력으로 걸러야 함.
- ⚠️ `product_master` 45-20-05 규격 꼬리가 아직 `*5`(박스내품 6 확정, spec_master만 8/28 정정) — ERP 정정 필요.
- **§8-B 신규 등록 체크리스트에 이지어드민 등록 단계 추가 필요**(이번 누락의 근본 원인).
