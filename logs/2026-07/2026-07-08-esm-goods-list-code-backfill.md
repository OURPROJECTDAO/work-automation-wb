# 2026-07-08 ESM 등록결과 관리코드 백필 (goods-list-result E열)

## 무엇 / 왜
ESM(G마켓) 등록 후 export `goods-list-result`의 **판매자관리코드(E열)가 빈 상태** — ESM 등록 양식에 판매자관리코드를 안 넣기 때문(esm-register 채우는 15열에 관리코드 없음). 사용자 요청으로 62건 E열에 관리코드 백필.

## 방법 (상품명 역매칭 → 박스 환원)
1. **상품명 정규화 매칭**: export C열(상품명)에서 ` 무료배송`·끝 `N개`·특수문자/공백 제거 → 통합 listing(esm/sikbom/smartstore/cashnote/baemin/allways/coupang/ali) `코드` 맵과 대조. **62/62 단일 매칭·충돌 0**. (등록명이 낱개 등재명 '1개'→'{박스내품}개' 변환분이라 끝 개수 제거가 매칭 핵심.)
2. **박스 관리코드 환원**: 매칭 코드 58건이 PC낱개(PCxxxxxx) → `PC` 제거·6자리 상품코드 → product_master 상품코드행 `관리코드`(박스). 4건은 이미 박스코드로 listing 등재 → 그대로.
   - ESM은 박스 등록(판매가=박스매입가 기준)이라 판매자관리코드=**박스 관리코드**가 정산 정합.
3. E열 값 텍스트 서식(`@`)으로 기입(앞자리0·점 `13-02.`·11자리 `86-66-05-0211` 보존).

## 검증
- 62/62 채움·빈 E 0·40열 보존. 환원유형 PC→box 58 / box그대로 4 = **62건 등록 로그(2026-07-06)의 'box코드4·PC58'과 정확 일치**(교차검증).
- listing_esm 마스터상품번호 직매칭은 0(스냅샷이 이번 등록 전 시점 — 상품명 매칭이 필요했던 이유).

## 다음 / 상태
- 산출 `/outputs/goods-list-result_관리코드기입.xlsx`. 코드 변경 0.
- 재사용 패턴: ESM(또는 타 채널) 등록 export에 관리코드 백필 시 = 상품명 정규화(무료배송/끝개수/특수문자) → 통합 listing 역매칭 → PC면 product_master 상품코드로 박스 환원.
