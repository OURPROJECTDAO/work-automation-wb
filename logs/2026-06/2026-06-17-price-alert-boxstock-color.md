# 2026-06-17 — 가격 변동 알림: 박스재고 컬럼 + 인상/인하 색상

## 무엇
데일리 대시보드 '💲 가격 변동 알림' 표에 **박스재고 컬럼** 추가 + **인상=빨강·인하=파랑** 색상 구분.
(app repo 커밋 00:14:41Z. 코드 검증·KB 기록만 본 로그에서 — 커밋 자체는 동일 PAT 계정.)

## 변경 (app repo, page-only)
- `app/pages/0b_데일리대시보드.py` 가격 변동 알림 섹션:
  - `_box_stock_lookup()`(product_master '박스' 컬럼·품절 알림판과 공유) → `_chg['박스재고']` 매핑(관리코드 NFC). 표 상품명 옆 배치·XLSX에도 포함·None은 '—'.
  - 방향 표기 ▲ 인상 / ▼ 인하(이모지 🔺🔻 → 텍스트 삼각형 — CSS 색 적용 가능).
  - pandas Styler `applymap(_dir_color, subset=['방향'])` — 인상 `#d11`(빨강)·인하 `#1565c0`(파랑)·bold. `format`(천단위 콤마·na_rep '—').

## 검증
- 파일 ast.parse OK. 박스재고=product_master '박스' 라이브(품절 알림판 동일 소스). 색상 Styler 텍스트색(▲▼는 텍스트 글리프라 색 반영).
- ⚠️ 상단 요약 메트릭 '🔻 인하'는 여전히 빨강계 이모지(st.metric 라벨은 CSS 색 불가) — 표 색상만 적용. 필요 시 아이콘만 교체 가능.
- ⚠️ Styler.applymap은 pandas 신버전 deprecated(→ .map) — 현재 동작(경고 가능). 에러 시 .map 교체.

## 다음 · 상태
- 상태: 배포됨(page-only). 데일리 가격 변동 알림에 박스재고·색상 표시 사용자 확인.
- 다음=요약 메트릭 인하 아이콘 색 통일(선택)·추가 데일리 인사이트.
