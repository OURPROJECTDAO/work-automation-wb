# 2026-06-12 — upload-monitor 박스재고/재고금액 임계값 필터

## 무엇 (page-only)
- 검색 행에 `박스재고 ≥`·`재고금액 ≥(원)` number_input 2칸 추가. 0=미적용, 값 이상만 표시(AND).

## 변경
- sc1~sc4 컬럼([3,1.5,1.6,1.4]): 검색·박스재고≥·재고금액≥·전채널제외포함. 행필터에 `view[view["박스재고"]>=min_stock]`·`>=min_amount`(>0일 때만). filter_sig에 임계값 포함(선택 위젯 리셋).

## 검증
- ast.parse OK. 박스재고≥10→686·재고금액≥300만→193·둘다→193·≥100&≥1000만→55. 정상.

## 상태
- 배포(page-only → Reboot 불요). 다음 = L4.
