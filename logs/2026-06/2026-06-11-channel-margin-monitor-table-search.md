# 2026-06-11 channel-margin-monitor — 표 검색 필터(상품번호·관리코드·상품명)

## 무엇
채널마진모니터 표에 검색창 추가. 상품번호·관리코드·상품명 **부분일치**로 행 필터링.

## 변경 (page만, core 무변 → Reboot 불필요)
- `st.text_input("🔍 검색")` → `q=cmm._nfc(search).lower()`. 3컬럼을 ' ||| '로 이어 `str.contains(q, regex=False)` 한 번에 매칭(경계 오매칭 방지 구분자). NFC 정규화(쿼리만; df 값은 이미 _nfc).
- 기존 필터(코드유형·마진미달만 등)와 AND 결합. 선택은 상품번호 기준 유지. `filter_sig`에 q 포함(검색 바뀌면 data_editor 리시드).

## 검증
- 실데이터: '맥콜'5·'우롱'2(상품명) / '29-00'1·'PC0058'4(관리코드) / '9672992613'1(상품번호) 정확. ast OK, 커밋 200.

## 다음 / 상태
- page-only → 재배포(1~2분) 자동 반영, Reboot 불필요.
