# 2026-06-16 천년경영업로드 — 박스(전체) 시트 박스코드 이상 탐지(검수 장치)

## 무엇 / 왜
사용자 사례: 어제(0615) 출력에서 PC005875([낱개1개] 명가 메밀칩 카레맛)가 **ESM전체**에 박스로 들어감. 누수 아님 — **소분목록(sub_list)에 아직 등록 안 한 낱개성 코드**라 파이프라인이 낱개로 못 보낸 것(사용자가 사후 소분목록에 추가). 이런 "소분 누락→박스 오업로드" 재발을 업로드 전에 잡는 **비차단 검수 장치** 신설.

## 근거 데이터(실측)
- 파이프라인은 **sub_list 멤버십만** 보고 낱개 분기 → 미등록 낱개성 코드는 전체 시트에 잔류(파이프라인이 못 봄).
- 박스 코드 판별 신호 = **코드 형태**: product_master 관리코드 4,227개 중 **영문자 포함 단 5개**(PC·BT·CMN·MGG·IMADE, 전부 특수 낱개성). 박스 코드는 전부 숫자-하이픈.
- `박스`/`낱개` 컬럼은 타입 플래그가 아니라 **재고 수량**(박스 상품도 박스=0 흔함) → 마스터 플래그 판별 폐기.
- 어제 전체 178행 대조: 영문코드 1건(PC005875)·상품명 `[낱개` 태그 1건(PC005875) **정확히 일치, 오탐 0**. 트레일링점 박스(`29-30.`,`52-55.`)·"30개입" 박스는 안 걸림.

## 설계 / 변경
- core `cheonnyeon_upload.py`: `detect_box_anomalies(sheets)` 신설 + `import re`. 전체 14시트 잔류 행만 스캔.
  - 신호 A(코드): 관리코드에 영문자 포함(`[A-Za-z]`).
  - 신호 B(상품명): 상품명에 `[낱개` 태그(`\[\s*낱개`).
  - 하나=검토, 둘 다=높음. 반환 list[dict]{시트·관리코드·상품명·신호·확신}. **파이프라인/골든 무영향(읽기만, run 시그니처 불변)**.
- page `1_파일처리.py` 천년경영 탭: `out,stats,sheets,_=cy.run()`로 sheets 수신 → detector 호출 → 결과를 session_state에 저장. 메트릭과 다운로드 버튼 사이에 `st.error`+표(`st.dataframe`)로 경고 + 소분목록 등록 유도 caption("기준데이터관리→천년경영업로드에서 추가 후 재실행").
- test `test_cheonnyeon_upload.py`: 합성 테스트(영문/낱개태그만 잡고 정상·트레일링점 박스 무탐지·확신/신호 라벨) + 실 파이프라인 결과 스키마 스모크.

## 검증
- ast.parse OK(3파일). 어제 실데이터 detector = 1건(PC005875·높음·코드영문+상품명낱개태그), 오탐 0. 주입 케이스 = 정상박스/트레일링점 무탐지, 영문코드·낱개태그 탐지(2건). 합성 테스트 독립 실행 통과.

## 배포 / 다음·상태
- 커밋: core 12d15cb8 · page 822118ad · test f93ce29c (work-automation-app).
- **상태: 운영. ⚠️ core 신규 함수 → Streamlit Cloud Reboot app 1회 필요**(import 모듈 캐시). Reboot 후 천년경영 탭 실행 시 경고 표 노출.
- 비차단(경고만) — 사용자 판단. 향후 후보: 영문 정상박스 5개 화이트리스트(`box_code_whitelist.csv`)·출력 xlsx 검수 시트·낱개 시트의 박스코드 역검출.
