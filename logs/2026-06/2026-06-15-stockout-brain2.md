# 2026-06-15 두뇌② 재고 지능 — 품절·재발주 예측 v1 (ADR 0020 2순위)

## 무엇 / 왜
데이터 backbone 완성 후 두뇌② 착수(ADR 0020 2순위·planned→완료). 현재고 ÷ 소진율 = 소진예측일,
매입현황 입고주기를 리드타임 proxy로 **재발주 시점** 판정. 반응형(품절 후)→예방형(품절 전).

## 입력 (전부 적립 완료 · 관리코드 직조인)
- **현재고**: product_master 최종재고[13](낱개) = 박스×박스내품+낱개 **일치율 100% 검증**. 재고금액=박스×박스매입단가.
- **소진율**: 매출자료(전채널·정산진실) 최근 N개월 수량 ÷ 윈도우 실일수. ★매출자료 관리코드 = **전부 base**(합포/PC/소분 0건 — ERP가 이미 낱개 분해, 실증) → 두뇌①의 '합성코드 미조인' 한계 **없음**. orders(온라인만)보다 매출자료(전채널)가 물리재고 소진에 정확.
- **입고주기(리드타임 proxy)**: 매입현황 관리코드별 중앙 입고간격(최근1년 861코드 산출·중앙 28일·p25 15·p75 52). 1회 이하면 기본 리드타임.

## 산출 (4구간 · 사용자 설계)
🔴 품절임박(소진예측일 ≤ 리드) · 🟡 곧재발주(≤ 리드×1.5) · 🟢 충분 · ⚪ 사장재고(매출0 & 박스재고>0). 재발주필요 = 소진예측일 ≤ 리드.

## 변경
- core `core/intelligence/stockout.py`(신규 ~210줄): `depletion_rate`(매출→일낱개소진·월낱개·최근판매일) · `restock_cadence`(매입 실입고→입고주기·최근입고일·횟수) · `forecast`(현재고÷소진→4구간·소진예측일·예상소진일자·리드타임·재고금액·재발주필요) · `summarize`. 비판매 제외 = 택배비(상품명) + 업로드감시 `EXCLUDE_MIDCAT` + 제외목록(상품코드) **재사용**(일관성). 음수재고 = 0 클램프(즉시품절).
- page `app/pages/9_재고지능.py`(신규): KPI 4종 · 3탭(재발주 대상 / 사장재고 / 전체) · 필터(현재고>0만 · 월낱개≥ · 재고금액≥ · 검색) · XLSX(관리코드 텍스트 서식). 사이드바=소진/입고 기간·기본 리드타임·기준일. **매출은 data repo 직read**(8_마진침식 `_sales_recent`가 app repo `_APP_API`로 읽는 잠재 버그 회피 — §아래).
- `app/streamlit_app.py` nav 등록(분석 그룹, 🔮 재고지능). ⚠️ core 신규 import → **Reboot app 1회**.

## 검증 (실데이터 · NOW=2026-05-31 · 소진 3개월 · 입고 12개월)
- forecast **1,089행**. 🔴 506(현재고>0 **260**=진짜 곧소진 / ≤0 246=이미소진·오버셀) · 🟡 76(전부 현재고>0) · 🟢 392 · ⚪ 115.
- 산식 정확: 현재고÷일소진 == 소진예측일(00-12 -778757/714.7=-1089.6 등 일치). 택배비 잔존 0 · 음수 소진예측일 0(클램프).
- forward 실례: 유동골뱅이中 3108낱개 ÷ 330/day = 9.4일 · 리드 12일 → 재발주(재고금액 1517만). 🟡 티오피더블랙 6876 ÷ 1032 = 6.7일 · 리드 5일.
- 사장재고 자본 **1.27억**(랩(산업용) 2467만 · 테이프 978만 등 비유동·매출0).
- 재발주필요 == 품절임박 일치. 리드출처 = 입고주기 805 / 기본 308.

## 전용 함정 (발견)
- **택배비 의사코드(`00`·`00-01`·`00-11`~`00-16`)**: product_master·매출에 섞임(택배비 회계항목). 00-12 'C타입택배비' 현재고 −78만 · 재고금액 −23억 → 미처리 시 품절임박 −19억 오염. **단 '0000-...'는 진짜 부자재 상품**(85개 중 다수) → '00 시작' 일괄제외 금지. **상품명에 '택배비' 포함 필터**가 정확(8개 전건 커버).
- **음수 박스재고/최종재고(이월·조정 drift, 98건)**: 소진예측일 음수 무의미 → `max(재고,0)` 클램프(즉시품절=0). 재고금액도 음수 박스 클램프.
- **현재고≤0 & velocity>0(246건)**: 이미 소진/오버셀. JIT 회전 빠른 상품은 박스재고 스냅샷이 상시 0/마이너스 → page '현재고>0만' 토글로 forward(260)와 분리.
- **8_마진침식 `_sales_recent` 잠재버그**: `_gh_raw`(=`_APP_API` app repo)로 `master/sales_*.parquet`를 읽음 → 매출은 data repo라 배포 시 404 가능성. 본 페이지는 `_data_raw`(data repo) 사용. (탭C 동작 여부 별도 점검 필요 — 백로그)

## 한계
- 소진 = 매출 base 직조인(박스재고>0 856코드 중 85.5% 매칭, 미매칭=매출0 사장재고로 포착). 매출 ≠ 물리출고 100%(반품·이월) → 추세 신호.
- 입고주기 = cadence proxy. **진짜 발주→입고 리드타임 = 발주자료 적재 후(1c 정밀화)**. 거래처 차원 리드타임 = 2022~23만(거래처코드 편중).
- 박스재고 절대값 = 흐름누적 오차 → product_master 앵커값 사용. 재고 스냅샷 추세(1b)는 검증용.

## 다음 / 상태
- ✅ 두뇌② v1 완료. ⚠️ **Reboot app** 후 동작.
- 향후 정밀화: ① 발주자료 적재(1c 진짜 리드타임·재발주 시점 정확화) ② upload-monitor 매트릭스에 '소진예측일' 컬럼 연계(반응형↔예방형 결합) ③ 소진율 채널 분해(margin_erosion.channel_velocity 재사용).
- **다음 = 두뇌③ 채널 가격 A/B**(채널명 정규화 선행) 또는 상품 360도 카드.
