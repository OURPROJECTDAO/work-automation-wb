# 2026-06-08 Phase 4 대시보드 — [데이터 추가] 증분 업로더 탭

## 무엇
대시보드 페이지(`app/pages/3_대시보드.py`)에 [➕ 데이터 추가] 탭 추가. 영업이익현황 .xlsx를 올리면 날짜구간 교체로 월 파티션에 누적되는 증분 업로더 UI.
(이전 세션 커밋 `2026-06-08T03:26 Phase4: 대시보드에 [데이터 추가] 탭`이 로그 누락된 채 세션이 끊겨, 본 로그로 소급 기록·루프 마감.)

## 왜
3개년 부트스트랩 이후 일/주/월 운영 적재를 챗·도구 없이 앱 안에서 직접 하기 위함. 저장 어댑터 `store.ingest`(날짜구간 교체)는 이미 구현·라이브 검증됨 → UI만 연결.

## 변경 (work-automation-app)
- `app/pages/3_대시보드.py`: 단일 → 2탭(`📊 대시보드` / `➕ 데이터 추가`).
  - 업로더 탭: file_uploader → `parse_sales`로 미리보기(행수 · 기간 min~max · 영향 월 목록) → 날짜구간 교체 경고 → "이 파일로 적재" 버튼 → `store.ingest(pat, repo, BytesIO)` → `load_sales.clear()`(@st.cache_data 무효화) → st.success(행수·갱신월수·구간) → st.rerun.
  - 시크릿 `[data] pat` 미설정 시 탭 내 에러 안내(앱 전체는 죽지 않음).
  - 유효 거래 0행(합계행만/빈 파일) 가드.

## 검증
- ast.parse OK(이번 세션 재확인). 두 탭 모두 `st.stop()` 미사용 → 빈 탭 함정 무관(탭 순서는 무해).
- `ingest`/`load_master` 경로는 store-adapter 로그에서 라이브 왕복·sha덮어쓰기·delete 검증 완료. UI 동작은 배포 후 사용자 확인 예정.

## 다음·상태
- 끊긴 루프 마감 완료: 본 로그 + state.md(완료 Phase·갱신) + workflows/dashboard.md(요약 stale 제거·관련 로그 4건 추가) 정합화.
- 대시보드 점진 확장 후보 미착수(사용자 우선순위 대기): ① 멀티연도 월별 매출 추이 차트 ② 이익률 KPI ③ 물류량(수량÷박스내품) ④ 이익/물류량 콤보(이중축) ⑤ 거래처 그룹 관리 탭.
