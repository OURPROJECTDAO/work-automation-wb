# 2026-06-22 데일리 대시보드 — 상단 메트릭 가독성 + 채널 요약 콤마/택배 박스개수

## 무엇 (3건, page-only)
1. **상단 4메트릭 잘림 해소** — 당일 매출/마진 큰 숫자가 카드 폭에 잘림(23,964,3…). ① `st.columns(4)` → `st.columns([3,3,2,2])`(매출·마진 넓게, 이상 건·역마진 좁게, 사용자 제안) ② 이 페이지 한정 CSS로 `stMetricValue` 폰트 1.55rem 축소 + white-space:nowrap·overflow:visible(자릿수 커도 안 잘림). core/ui.py엔 metric value 폰트 미지정(스트림릿 기본 대형)이라 잘렸음.
2. **채널별 요약 콤마** — NumberColumn(format="%d") → **Styler `.format("{:,.0f}")`**(이 파일 가격변동 표가 이미 쓰는 검증된 패턴). 매출(net)·원가·마진·품목수 천단위 콤마. 마진%는 {:.1f}.
3. **택배 → 박스개수** — 택배(원) 대신 `(택배 / 2700).round().astype(int)` 새 컬럼 '택배(박스)'. 사용자 요청(40500→15). flat 2700 ÷ 검산: 식봄 629100/2700=233·ESM 251100/2700=93·스스 645300/2700=239 → 캡션 '실제 박스수'와 일치(택배비=박스수×2700이라 정확).

## 변경 파일
- `app/pages/0b_데일리대시보드.py` (page-only). 커밋 7d0fefe.

## 검증
- ast.parse OK · src.count(old)==1 전 2건(메트릭 블록·채널요약 블록).
- 택배/2700 박스수 캡션 _chb와 정합 확인.

## 다음·상태
- page-only → 재배포 자동반영, **Reboot 불요**. 화면 확인 대기(매출/마진 끝까지 표시·요약 콤마·택배 박스수).
- ★ 택배 박스수는 flat 2700 고정 가정(설정에서 채널 flat 바꾸면 어긋남 — 현재 전 채널 2700이라 정확). 캡션의 _chb(물리 박스수)가 더 정확한 원천이나 사용자 요청대로 ÷2700.
