# 2026-06-17 캐시노트 상품 등록 채널 셋업 (cashnote-register 신설)

## 무엇
- 상품 등록 클러스터(02)에 **캐시노트(KCD 마켓플레이스)** 를 4번째 등록 채널로 추가(챗 네이티브). 양식·카테고리표 확보 + 워크플로우 문서 신설. 첫 배치 대기.

## 왜
- 캐시노트는 마진모니터(6%·OFR/SKU·배송정책)·송장처리엔 있으나 신규 상품 일괄등록엔 없었음. 로드맵 진행 — 등록 채널 확대.

## 변경
- 사용자 제공 양식 `marketplace-bulk-product-create_일괄등록예시.xlsx` 분석:
  - 시트 `상품` 42열(A:AP), r1섹션·r2필드명·r3필수/선택·r4안내·데이터 r5부터(1~4행 보존). 부속 시트 3종(카테고리 안내·고시 항목·필드값).
  - 채우는 ~13열 확정: A상품명·C대표옵션Y·D카테고리·H검색어·K판매가·M재고9999·**P합포N(주문N개당 배송비)**·R관리코드·S과세·T진열Y·X대표이미지·**Y상세이미지(평문URL)**·Z배송/반품정책(DVP212991/RTP078367 고정)·AA상품군번호21.
  - 스마트스토어와 차이: ①배송비템플릿코드표 없음→합포N을 P열 직접 ②상세이미지 `<img>` 래핑 없는 평문 URL ③자체 카테고리 53종 체계.
- app repo `reference/cashnote_category_food.csv` 신설(가공식품 53종 — `상품 필드값` D열 정본 드롭다운 문자열을 코드로 조인, l1/l2/l3는 `카테고리 안내` 시트. 080501/2 1차분류 '라이프' 보정).
- app repo `reference/cashnote_bulk_template.xlsx` 신설(양식 원본 바이트 — 데이터검증/드롭다운 보존).
- KB `workflows/cashnote-register.md` 신설. product-registration-common 채널목록·state 인덱스·INDEX 목록·systemmap 노드(registration cluster, status partial)+edge·roadmap 갱신.

## 검증
- 카테고리 53종 ↔ 필드값 D열 전건 매칭(미매칭 0). 예시 010809 정본값 일치 확인.
- 양식 구조 openpyxl 덤프로 42열·행구조·셀서식(K/L/P/Q/R=#,##0) 확인. ⚠️ 데이터검증 extension 미지원 경고(저장 시 제거) — 출력 무해 예상, 첫 실업로드로 검증.

## 확정 사항 (사용자 2026-06-17)
1. 입력=스마트스토어 10열 시트 공용 + cashnote_category_food.csv + workflows 신설(코드 변경 없음).
2. 카테고리 매핑 = AI 제안 + 사용자 확정(스마트스토어 동일).
3. 검색어 키워드(H) = 상품명 그대로, 단위 suffix만 제거.
4. 정가(L)·고시정보값(AB~AP) = 비움(양식 예시 비어있음·선택).
5. 수수료 6% (ADR 0014 재사용).

## 다음 · 상태
- **준비 완료. 사용자가 등록 상품(10열 시트) 주면 첫 배치 생성.**
- 첫 배치 시 확인: (a) openpyxl 저장본 캐시노트 업로더 허용 여부 (b) 고시정보값 비움 충분 여부.
