# 2026-09-02 — 50-72-05 선물세트 셋팅 검증 + 라인업 41종 5파일 전수 재점검

## 무엇
사용자 요청: "50-72-05 선물세트로 천년경영 업로드시 빠지게 되어있는지 확인하고 안됐으면 해달라".
→ **이미 정상 셋팅. 조치 불필요.** 덤으로 라인업 전수 재점검까지 수행, 결손 0건.

## 왜
2026-08-28에 45-10-10이 `sub_list` 누락으로 ERP 낱개 시트로 안 빠지던 사고가 있었고,
`detect_box_anomalies`가 선물세트는 **신호 A(영문자 코드)·B(`[낱개` 태그) 둘 다 미해당**이라
경고 없이 조용히 틀리는 사각지대다. 그래서 신규/의심 코드는 매번 실측 확인이 필요하다.

## 변경
**없음.** 기준데이터·코드 전부 무변경(확인만).

## 검증

### 1) 기준데이터 5파일 존재 확인 — 50-72-05 (동원참치 S15호/6세트(30))
| 파일 | 값 |
|---|---|
| `sub_list` | 낱개개수 `1` · 원코드 `50-72-05`(자기참조) · 구분 `선물세트` |
| `unit_list` | 구분 `선물세트` · 박스내품 6 · 입력값 0.1666… |
| `product_attributes` | 식품음료 `선물세트` · 최종분류 `선물세트` |
| `logistics_classification` | `선물세트` |
| `spec_master` | `(살6+야3+고3+Dha3)*6` |
| `sobun` | 미등록 (선물세트 규칙대로 정상) |
| `baseline_margin` | 10채널 전건 0.04 |

### 2) ★ 코어 실행 골든 테스트 (문서 대조가 아니라 실제 `process()` 구동)
`core/workflows/cheonnyeon_upload.py` 를 받아 합성 발주 5행으로 `process()` 실행:
- 50-72-05 → **스마트스토어·ESM·캐시노트 3채널 전부 낱개 시트로 이동** ✅
- 이동 후 `B=50-72-05`(자기참조라 코드 불변) · `I=1` · `J=D×1` · `K=H` → **무변환 통과** ✅
- 대조군: 45-11(선물세트) → 낱개 이동 / 31-01-04(코카콜라, 일반) → 전체 시트 잔류 ✅
- `detect_box_anomalies` 빈 배열 ✅

★ 분기 근거 재확인: `process()`의 전체↔낱개 분기는 **`sub_map` 멤버십이 유일 근거**
(`(units[unit] if r["B"] in sub_map else keep)`). `unit_list.입력값`(0.1666)은 이 분기에 쓰이지 않고,
낱개 변환에 쓰이는 건 `sub_list.낱개개수`(=1) 다. 그래서 선물세트는 J=D×1 로 통과한다.

### 3) 라인업 전수 재점검
`reference/giftset_lineup_2026.csv` **41종** × 5파일 전수 대조:
- **결손 0건**
- 자기참조 규칙(낱개개수 1 · 원코드=자기자신 · 구분=선물세트) **40/40 통과**
→ 2026-08-28 결손 6종 보정 이후 새로 생긴 구멍 없음.

## 다음·상태
- **완료. 펜딩 없음.**
- 신규 선물세트 등록 시 `giftset-season-ops.md` §8-B 체크리스트(기준데이터 1~6 → 채널 7 → hapo 8 → 이지어드민 9) 유지.
- 주기 점검 레시피 = 라인업 정본 × 5파일 전수 대조 + `process()` 골든 실행(위 2번). 이번에 재현 확인됨.
