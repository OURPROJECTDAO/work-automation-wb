# 2026-09-08 선물세트 시즌 관제판 — 재고 경보에 상품 상태 열 추가 (D-17)

## 무엇 / 왜
사용자: "재고 경보 다 좋은데, 현재 상품 상태에서 품절처리로 그냥 걸어버릴 수 있는 거? 열을 하나 추가해줘."
→ **재고 숫자만으로는 '재입고가 오는 품절'과 '그냥 끝난 품절'이 구분되지 않는다.**
소진예측일·마감후잔여세트는 둘을 똑같이 취급하므로, 품절 임박 표의 "추가 매입 검토"가
재입고 경로가 없는 품목까지 섞어서 나온다(50-71-02 = 동원 재고소진으로 재입고 불가 확정,
45-10-10 = 시즌 비운영 확정 — 둘 다 KB엔 있으나 화면엔 안 보였다).

구분 4종(사용자 지정) = **판매중 · 일시품절(재입고예정) · 추가입고없음 · 판매중지** + `미지정`.

## 설계
**데이터로 유도 불가 → 참조 CSV 영속 + 페이지 인라인 편집.**
재고>0/재고=0은 계산되지만 "재입고가 오는가"는 영업 판단이라 자동 산출 대상이 아니다.
- 신규 `reference/giftset_status.csv` — `관리코드,상태,비고,갱신일` · UTF-8 BOM.
  **파일이 없어도 정상 동작**(404 → 전건 `미지정`, 첫 저장이 sha 없이 생성).
- `product_attributes`/`unit_list` 같은 기존 기준데이터에 넣지 않은 이유 = **시즌 한정 상태**라
  전 워크플로우 공용 기준데이터를 오염시킨다. 시즌 종료 시 이 파일만 비우면 된다.

## 변경 — `app/pages/14_선물세트시즌.py` 단일 파일 (커밋 `c076dcd5`)
**page-only → 재배포만, Reboot 불요**(core 무변경).
1. `_status_map()`(ttl 600) · `_status_current()` · `_save_status()` 신설.
2. board 산출 직후 `b["상태"]`·`b["상태비고"]` 병합 → 전 탭에서 사용 가능.
3. **재고 경보**: 상단에 `상태 필터`(비우면 전체) + `추가입고없음·판매중지 숨기기` 체크박스 →
   `bb` 로 4개 표(이월 위험·품절 임박·재고 소진·무매출/저마진) 전부 필터. 각 표에 `상태` 열.
   품절 임박 캡션이 **재입고 여지 있는 종수만** 추가매입 검토 대상으로 셈.
4. **관제판** 표·XLSX에도 `상태` 열(상품명 다음).
5. **상품 상태 설정** 섹션 — 검색 + `st.data_editor`(SelectboxColumn) + 저장.

### 함정 대응
- **`st.form` + `form_submit_button`** — data_editor 와 폼 밖 버튼을 쓰면 마지막 셀 편집이
  누락된다(pitfalls 2026-07-07). 폼으로 묶어 한 번에 확정.
- **업서트 저장** — 화면에 보이는 행만 올려도 나머지 행은 원격 최신본에서 병합 보존.
  `미지정` 은 행 삭제. 변경 없으면 PUT 자체를 안 함(멱등).
- **CDN stale 방어**(pitfalls 2026-07-20) — 저장 직후 원격 raw 가 옛 본문을 돌려주면
  방금 저장한 값이 되돌려진다. → 세션 `_gs_status_snapshot`(마지막 기록 전체 맵)이
  원격보다 **우선**, 원격에만 있는 신규 코드는 살리되 `_gs_status_deleted` 는 되살리지 않음.
- sha 재취득 + 409/422/403 backoff 3회.
- 관리코드는 CSV 텍스트로만 다뤄 날짜 변환(`45-12-05`→`2045-12-05`) 없음.

## 검증 (실제 함수 소스를 스텁으로 구동)
`ast.parse` 통과. `_status_map`/`_status_current`/`_save_status` 소스를 AST 로 떼어내
가짜 GitHub(원격 본문·sha·PUT 캡처)로 실행:
| # | 케이스 | 결과 |
|---|---|---|
| ① | 신규 파일 생성 | sha 미포함 PUT · **BOM 있음** · `미지정` 행은 미저장 |
| ② | 같은 값 재저장 | PUT 없음("변경된 항목이 없습니다") |
| ③ | 1행만 편집 | 나머지 2행 보존 · 기존 파일은 sha 포함 |
| ④ | `미지정` 되돌리기 | 행 삭제 |
| ⑤ | **원격이 stale 옛 본문** | 직전 저장분 유지 · 해제 코드 부활 안 함 · 원격 신규 코드 생존 |
| ⑥ | 헤더·코드 텍스트 | `관리코드,상태,비고,갱신일` · `45-21` 원문 보존 |

★ 1차 테스트에서 "BOM 없음"이 떴는데 **테스트 쪽 이스케이프 오류**였다(f-string 안
`b'\\xef...'` 가 리터럴 백슬래시로 들어감). 코드는 정상. — 검증 실패는 코드가 아니라
검증기를 먼저 의심할 것.

## 다음 · 상태
- 재배포 후 사용자 실사용 확인 대기(상태 지정 → 저장 → 필터 반영).
- **KB 상 이미 확정된 상태 후보**(사용자가 화면에서 지정하면 됨):
  `50-71-02` 추가입고없음(동원 재고소진·잔여 23박스 소진으로 종료, 2026-08-31 확정) ·
  `45-10-10` 시즌 비운영 확정인데 재고 170만·매출 44.6만·마진 −7.6%(판매중지/추가입고없음 판단 필요) ·
  `50-24-01` 재고 0 · `50-72-01` 재고 소진.
- 상태를 지정하면 §12 의 "저마진 채널부터 닫고 고마진에 남긴다"(50-71-02 잔여 노출 11리스팅)
  판단을 화면에서 바로 걸 수 있다.
- 미착수(이 세션 후보에서 보류): 나들 저마진판 · 채널 등재갭판 · 사라진 리스팅 diff · D-15 트리거판.


---

## 후속 ② — 판매촉진 플래그 + 초기 상태 12건 (같은 날)

### 판매촉진 = 상태와 **직교하는 축**
사용자: "판매 촉진 / T시리즈 판매 너무 안돼서 빨리 팔아야할듯 / J호".
`촉진`은 "팔 수 있는 상태인가"(상태 축)가 아니라 **"밀어내야 하는가"** 라서 상태 값으로
넣으면 안 된다(`판매중`이면서 촉진일 수 있다). → 같은 CSV에 **별도 `촉진` 열**(Y/공백).
- `_STATUS_COLS` = `관리코드,상태,촉진,비고,갱신일`. 구 4열 파일도 그대로 읽힘(`.get` 폴백).
- **행 삭제 조건 변경** — 상태 `미지정` **이면서 촉진도 꺼져 있을 때만** 삭제.
  촉진만 켠 행(상태 공란)이 살아남아야 하므로 `_status_map` 도 `c and (상태 or 촉진)` 으로 완화.
- 화면: 재고 경보 최상단 **[판매 촉진 대상]** 섹션(마감후잔여세트·이월재고금액·재원잔액 대비
  ·XLSX) + 재고경보·관제판 `촉진 대상만` 필터 + 편집기 `CheckboxColumn`.
- ★ **노출 경고** — 촉진 대상 중 `주요채널계 < 사업부계 × 20%` 이면
  "가격이 아니라 노출·등재 문제일 수 있음"을 띄운다. J호(전체 9,345만 중 온라인 513만)가
  정확히 이 패턴이라 규칙화했다. **가격을 깎아도 안 보이면 안 팔린다.**

### 검증 (촉진 축 추가분)
① 상태+촉진 동시 저장 ② 멱등 ③ **촉진 해제 → 행 삭제** ④ **상태만 해제하고 촉진 유지 → 행 생존**
⑤ 헤더 `관리코드,상태,촉진,비고,갱신일` · BOM. 전건 PASS.

### 초기 상태 12건 커밋 (`reference/giftset_status.csv` · `9daaaf69`)
사용자 지정:

| 코드 | 품목 | 상태 | 촉진 |
|---|---|---|---|
| `50-75-05` | 튜나리챔102호 | 추가입고없음 | |
| `50-71-05` | 스페셜S호 | 추가입고없음 | |
| `50-75-02` | 튜나리챔77호 | 추가입고없음 | |
| `50-71-03` | 프리미엄60호 | 추가입고없음 | |
| `45-21` | 스팸8호 | **일시품절(재입고예정)** | |
| `01-17` | 청정원 NH2호 | 추가입고없음 (일단 판매) | |
| `50-72` | 참치M12호 | 추가입고없음 (일단 판매) | |
| `51-30`·`51-30-01`·`51-30-02`·`51-30-03` | T12·T14·T21·T18호 | — | **Y** |
| `45-07-06` | J호 | — | **Y** |

★ 사용자 원문은 5종 모두 "품절"이었으나, 되물으니 **"스팸8호 외에는 재입고 예정 없음,
스팸8호는 재입고 언제될지 몰라서 품절"** → 4종은 `추가입고없음`, `45-21` 만
`일시품절(재입고예정)`(재입고는 오되 시점 미정 → 리스팅 유지)로 해석해 기입.
**해석이 들어간 유일한 건이므로 화면에서 한 번 확인 필요.**

### ★ 이 배정이 뒤집은 것 / 충돌
1. **`45-21` 스팸8호** — 2026-09-03에 "재고는 계속 들어온다"(사용자 확정)로 음수 재고 경고를
   껐고, 9/7 나들 21,700→22,200 인상을 집행한 코드다. 이제 품절 → **나들 잔여 4,167세트
   가정이 무효.** 9/4 하루 1,095세트까지 나가던 물량이 멈춘다.
2. **`50-71-03` 프리미엄60호** — 9/3부터 이월돼 온 "나들 22,000 = 마진 정확히 0% → 22,700
   인상" 권고가 **품절로 자연 소멸**. 미집행 상태로 시즌을 넘긴다(누적 900세트 1,998만 / 이익 18만).
3. 🔴 **T시리즈 촉진 vs 브랜드 최소단가 충돌** — 2026-09-03에 자사몰 40리스팅을 거래처
   「최소단가표」에 맞춰 **올렸고 T라인 4종이 그 핵심**이었다(T12 1.66→10.44 · T14 5.60→10.48 ·
   T18 3.22→10.54 · T21 4.23→10.58%). **최소단가가 하한이라 인하로는 못 푼다.**
   8/31 진단의 "A군 = 매입가가 작년 판매가를 추월 → 가격으로 못 푼다"와 결론이 일치한다.
   → 남은 수단 = ① 거래처 최소단가 **예외 요청** ② 최소단가표에 없는 채널·품목 확인
   ③ 최소단가 위에서의 쿠폰·광고·노출. 재고는 T14 4,913만 · T18 3,377만 · T12 2,554만 ·
   T21 2,296만으로 이월위험 상위권.

### 다음
- **촉진 5종 노출 진단**(사용자 선택) — 채널별 세트당가 정규화 · 등재 깊이 · 최근 7일 판매로
  "가격이 막혔나 노출이 막혔나" 분리. J호는 노출 쪽이 유력.
- `45-21` 상태 해석 확인 · 나들 잔여 추정 재산정.


---

## 후속 ③ — 사용자 정제본 반영 + 판정 수치화 + 미분류 열 (커밋 `210c76ad`·`308622d1`)

사용자가 `선물세트_관제판_20260908.xlsx` 를 직접 손봐서 업로드 + 추가 피드백.

### 사용자가 바꾼 것 (전수 대조로 확인)
1. 금액 18열 → 회계 서식 `_-* #,##0_-;\-* #,##0_-;_-* "-"_-;_-@_-`
2. 마진율 `0.0%` · YoY/작년진도 `0%` · 소진예측일 `0.0` · 마감후잔여세트 `0`
3. 헤더 rename `마감후잔여세트` → **`마감후잔여세트(예상)`**(추정치 명시)
4. 열 폭 21개
5. **`온라인B2B계`·`사업부계`·`결손` 3열에 전 행 `필요없음` 기입** = 데이터가 아니라 **빼라는 표시**
   (처음엔 값으로 읽고 `n()` 헬퍼가 0으로 캐스팅해 "사업부계 전건 0 = 버그"로 오진할 뻔했다.
   문자열이 든 열을 훑어보고서야 의도를 파악 — **숫자 열에 문자열이 섞이면 값이 아니라 메모를 의심할 것**)

### ★★ 그 과정에서 발견한 진짜 결함 — 시즌매출 5.55억이 표에서 사라진다
40행 합계: **시즌매출 15억 3,011만 vs 채널 12열 합 9억 7,506만 = 5억 5,505만 갭.**
원인 = `scope_sales` 는 `GRP_SKIP`·`GRP_OUT` 만 제외하고 **`GRP_UNK`(미분류)는 남기는데**,
`channel_columns()` 에 미분류가 없어 **`시즌매출`엔 들어가고 채널 열엔 안 잡힌다.**
2026-09-07 이관 검증 때 "채널 합계 == 시즌매출 diff 0" 이었던 건 그날 미분류가 0곳이었기 때문.
→ **`channel_columns()` 에 `GRP_UNK` 추가.** 이제 채널합이 시즌매출과 맞아떨어진다.
⚠️ 파생 영향(미해결): `시즌매출`·`마진율`·`YoY`·`판정`이 **미분류(대부분 오프라인 추정) 포함
값**이다. 오늘 업로드가 전사 export였을 가능성이 크다 — 사용자 확인 대기.

### 판정 수치화 — `core.prescribe()` (사용자 요구: "AI 판단이 아닌 숫자적 판단")
기호 `S=세트재고(0클램프) · v=일평균세트7 · R=잔여시즌일 · D=S/v`.

| 판정 | 권장 수치 | 근거식 |
|---|---|---|
| 🔴 인하 집행 | 마진 −Δ%p (가격 −x%) | `Δ = min(마진여력, 이월비용률) × min(k−1, 1)`, `k = (S/R)/v` |
| 🔵 인상 검토 | 마진 +Δ%p (가격 +x%) | `Δ = (계획마진 − 현재마진) × 0.5 × 여유율`, 여유율 `= 1 − D/R` |
| 🟡 추가매입 | +N세트 (B박스) | `N = v×R − S`, `B = ceil(N/박스내품)`, 매입액 `= N × 세트원가` |
| 🟢 유지 | 이월 예상 / 촉진 인하 −Δ%p | 마감후잔여세트 표기. **촉진 대상이면 인하 여력을 계산** |

- **상한의 근거**: 인하 상한 = **이월비용률 3.3%p**(§7 — 그보다 크게 깎아 지금 터는 건 이월보다
  손해) · 하한 = **절대하한 3%** · 인상 앵커 = **계획마진 8.8%**, 절반스텝은 두뇌④(ADR 0026) 관례.
- 가격 환산은 원가 고정 가정 `p=C/(1−m)` → `Δp/p = (1−m)/(1−m′) − 1`.
- **상태 반영**: `판매중지`→가격 조정 대상 아님 · `추가입고없음`+🟡→"추가매입 불가, 잔여 소진" ·
  재고 ≤ 0 + 🔵 → "팔 물건이 없어 가격 조정 무의미"(45-21 −7,315세트·50-71-03 −1,065세트).
- ★ **🟢 유지의 구멍을 메웠다** — T12호(`51-30`)는 소진 48.6일 ≫ 잔여 12일인데 YoY 110%라
  4분면상 🟢가 나오고 **아무 말도 안 했다.** 이제 촉진 대상이면
  `촉진 인하 −3.3%p (가격 −3.4%) · 마감 후 1,356세트(3,580만) 남음` 으로 산출된다.

### 헤더 직관화 (`DISPLAY_NAMES`, 내부 컬럼명은 유지하고 표시만 치환)
`작년동기_달력`→**작년 이맘때까지** · `YoY`→**작년 이맘때 대비** ·
`작년시즌`→**작년 시즌 전체** · `작년진도`→**작년 전체 대비** · `마감후잔여세트`→**(예상)** 추가.

### XLSX 내보내기 전면 개편
회계 서식·비율·수량 `#,##0`·코드 텍스트(`45-12-05` 날짜 변환 방지)·헤더 볼드+음영·
**틀고정 A2·자동필터**(사용자가 안 넣은 것, 40행 표라 추가)·열 폭. 촉진 대상 XLSX도 동일 적용.

### 검증
- `ast.parse` 2파일. `prescribe` 를 **업로드본 실제 40행에 그대로 구동** — 🔴2/🟡18/🔵16/🟢4 전건
  문장 생성, 수치 손검산 일치(51-30-02 재고 420÷일 25.1 = 16.7일 > 12일 → 1.39배 → 5.1%p·3.3%p
  중 min × 0.39 = −1.3%p → 가격 −1.4%).
- `_to_xlsx` 를 AST로 떼어 실제 실행 → 36열 서식·폭·freeze `A2`·filter `A1:AJ41` 전건 확인.

### 다음 · 상태
- ⚠️ **재배포 + Reboot app 1회**(core 변경).
- 🔴 **오늘 업로드가 전사 export인지 인터넷사업부 export인지 확인 필요** — 전사면 미분류 5.55억이
  오프라인이고, `마진율`·`YoY`·`판정`이 전부 그만큼 왜곡돼 있다.
- 🚀 T14호(`51-30-01`)는 오히려 🟡 추가매입(+379세트) — **주요채널 33만 vs 나들 1,869만**이라
  온라인만 죽은 것. "T시리즈가 안 팔린다"는 체감과 데이터가 갈린다(9/7 '채널이 갈아탔다'와 동형).


---

## 후속 ④ — 인터넷사업부 export = 분류 사전 (커밋 `515e3447`)

후속 ③에서 드러난 "미분류 5.55억" 의 근본 해법. 사용자가 **인터넷사업부 매출만 뽑을 수 있다**고
해서, 그 파일을 **매출이 아니라 소속 판정의 오라클**로 쓰는 경로를 만들었다.

- 파케이는 전사 유지(대시보드·재고지능 공용) → 사업부 export 는 **적재하지 않는다.**
- `_division_from_export()` — export 내 미매칭 상호명 → 온라인B2B / 같은 기간 파케이에만 있는
  상호명 → 사업부밖. 이미 분류된 건 후보에서 자동 제외.
- 데이터 적재 탭을 **①전사 적재 / ②분류 사전** 두 섹션으로 분리. 기존 🚫 가드 문구도 ②를 가리키게 수정.
- 전사 export 오업로드 감지 = 거래처 수가 같은 기간 파케이의 95% 이상이면 경고.

**검증(합성 골든)** — 사업부 8곳 중 오프라인 2곳을 뺀 export 를 넣어:
b2b 후보 = {태성전기, 빅세일컴퍼니}(표기규칙에 잡히는 지마켓·나들·자사몰, 제외계정 로켓창고는 자동 배제) ·
out 후보 = {푸드앤플러스, 삼일유통} · 일부를 화이트리스트에 넣고 재실행하면 그것만 후보에서 빠짐 ·
전사 export 를 넣으면 오프라인까지 b2b 후보가 되고 경고 조건(export≥파케이×0.95)이 성립. 전건 PASS.

**다음** — 사용자가 사업부 export 를 ②에 올리면 미분류가 0 이 되고, 그때의 `마진율`·`YoY`·`판정`이
사업부 스코프 기준으로 정확해진다. 그 값으로 D-15(9/10) 판정을 봐야 한다.


---

## 후속 ⑤ — 소진 임박(3일) 별도 경보 · G호 상태 정정 (커밋 `a491d1d3` 외)

### G호 정정
`45-07` 을 사용자 지시대로 `추가입고없음` 으로 넣었는데 **"이미 판매중지"** 라고 정정받아
`판매중지` 로 교체(`bf6bc97b`). 함께 **`판매중지` 근거 문구를 판정 맥락에 맞게 수정**(`1e36d502`) —
🟡 추가매입 검토인 코드가 판매중지면 "가격 조정 대상 아님"만 나와 **'추가매입 얼마?'라는 질문에
가격으로 답하는** 꼴이었다. → "가격·추가매입 모두 대상 아님 + 잔여 N세트(금액)는 이월 대상".

★ **내 오독 기록** — G호 소진예측 0.4일을 보고 "오늘내일 바닥나고 수요가 증발한다"고 보고했으나,
**이미 판매중지라 그 0.4일은 최근 7일 이력으로 계산된 잔상**이었다. 남은 48세트는 기회손실이
아니라 이월 물량이다. **상태를 모르는 채 소진예측일만 읽으면 긴급도를 반대로 읽는다.**

### 소진 임박 경보
사용자: "소진예측일이 3일 미만인 건 따로 알려줘야 해."
기존 `품절 임박` 은 **소진예측 < 잔여시즌일(12일)** 이라 24종이 걸려 긴급도가 묻혔다.
→ **임계 `임박_일수`(기본 3)** 를 시즌 파라미터로 신설하고 별도 축으로 분리.

- **위치 = 탭 밖 상단 배너 + 재고 경보 탭 맨 위**(촉진보다 위). 탭을 열지 않아도 보인다.
- 조건 = `세트재고>0 ∧ 일평균세트7>0 ∧ 소진예측일<임계 ∧ 상태≠판매중지`.
  재고 ≤0 은 이미 `재고 소진` 섹션이 잡고, 판매중지는 조치 대상이 아니다.
- **상태로 조치가 갈린다** — 재입고 경로 있음 → 즉시 발주 검토(권장 수량은 `권장 조정` 열) /
  `추가입고없음`·`판매중지` → 발주 불가, **저마진 채널부터 노출을 내려** 품절 노출을 줄인다.
- ★ **상태 필터와 무관하게 항상 전건** 표시한다. 경보를 필터로 숨길 수 있으면 경보가 아니다.

**실측(2026-09-08 업로드본, 잔여 12일) = 9종** — `45-07-03` N호 재고 92세트/일 260.4세트
**0.4일**(시즌매출 6,972만) · `45-08` 0.8일 · `50-71-02` 1.2일 · `45-05` 1.5일(7,793만) ·
`50-20-01` 1.6일 · `45-28` 1.8일 · `50-71-06` 2.4일 · `45-27` 2.6일(7,037만) · `45-07-05` 2.7일.
**9종 전부 상태 `미지정`** — 재입고 가능 여부가 안 잡혀 있어 발주/노출정리 판단이 안 선다.
제외 = 재고 0 이하 7종(`45-21`·`50-71-03`·`50-75-02`·`45-10-09`·`50-71-05`·`50-75-05`·`55-08`·
`50-72-01`) + 판매중지 1종(`45-07`).


---

## 후속 ⑥ — 발주량 산출 앱 이관 (커밋 `b292e15d`)

챗에서 만든 발주량 표를 그대로 페이지에 넣었다(재고 경보 탭, 소진 임박 바로 아래).

### 계산
`일평균(박스) = 최근 N일 판매세트 ÷ N ÷ 박스내품` ·
`N일안 = ⌈일평균 × 단기일수 − 박스재고⌉` · `전체필요 = ⌈일평균 × 잔여시즌일 − 박스재고⌉` (음수는 0).
대상(소진임박/촉진/전체)·평균 산출 일수·단기 발주 일수·제외 코드를 화면에서 고른다.

### ★ `res["scoped"]` 를 쓰면 안 된다
`scope_sales` 는 **`사업부밖`(오프라인)을 이미 제외**하므로 거기서는 '전사 기준'이 안 나온다.
→ `_recent_qty()` 가 **원본 매출 프레임에서 상호명 단위로 다시 분류**한다(행마다 호출 안 함).
`제외` 계정(로켓창고)만 빼고, 사업부 = DIVISION_GROUPS · 전사 = 미분류·사업부밖 포함.

### ★★ 사업부 기준만 보면 과소 발주가 된다
**재고는 채널 공용**인데 판매속도만 사업부로 좁히면 오프라인 수요가 계산에서 사라진다.
실측(2026-09-08, 최근 3일): `45-05` 스팸6호 사업부 203세트 / 전사 903세트 = **비중 22%** →
사업부 기준 5일안 **0박스** vs 전사 기준 **271박스**. `45-28` 10% · `45-08` 6% 도 동종.
→ 화면·엑셀 모두 **전사 기준 3열과 `사업부%` 를 나란히** 두고 경고 문구를 붙였다.

### 엑셀 (A4 가로 1장)
2단 그룹 헤더(`인터넷사업부 기준` / `전사 기준` merge)로 라벨을 짧게 → 폭 합계 **104자**
(A4 가로 인쇄폭 ≈105자)로 축소 없이 들어간다. `fitToWidth=1`·`print_title_rows="1:2"`·
`freeze_panes="C3"`. 값이 아니라 **수식**이라 가정 셀(단기일수·잔여시즌일·평균일수)만 고치면
전체가 재계산된다. 원자료(세트) 2열은 숨김. `recalc.py` 0 errors.

### 사용자 판단 (2026-09-08)
제외 3종 = `45-28` 복합100호(3일 22세트) · `45-08` 스팸1호(10세트) · `50-71-02` 스페셜5호(0세트).
★ 사용자 업로드본에는 `45-05` 도 빠져 있었으나 **지시에는 없어 남겨두고 근거를 제시**했다
(사업부 기준 0이지만 전사 기준 5일 271박스·시즌매출 7,793만). 확인 대기.

### 검증
`_recent_qty` 를 실 파케이(2026-08·09)에 돌려 6종 (사업부/전사) 세트가 **직전 손계산과 전건 일치**.
`_order_xlsx` 생성 → `recalc.py` 0 errors · 페이지 `ast.parse` 통과.
개발 중 자기 버그 1건 — 제외 코드 라벨을 **이미 걸러낸 프레임**에서 찾아 `IndexError` 날 자리였다.
`_pool`(제외 전) / `_src`(제외 후)로 분리. 필터와 라벨 조회의 프레임이 다르다는 걸 놓쳤다.

### 다음
- 발주 실행은 **CJ·동원 시즌 생산 마감 여부와 리드타임 확인이 선행**. 확인되면 리드타임을
  파라미터로 받아 '발주해도 못 받는 물량'을 빼는 계산까지 가능.
- 제외 3종은 상태를 `추가입고없음` 으로 걸어두면 매번 손으로 뺄 필요가 없다.


---

## 후속 ⑦ — S15호(50-72-05) hapo 누락 해소 + 전 채널 스윕 (`2e9e1a74`·`c0e7821b`)

사용자 지적 "채널마진모니터 ESM에서 S15호 hapo 처리 안 된 것 같다" → 실물 확인 결과 사실.

### 진단 (실제 코어 실행)
`cmm.compute_listing` 을 저장 listing 에 그대로 돌려 확인:
`50-72-05` ESM 4리스팅 중 **1세트 2건이 매입가 148,500(박스단가 그대로) → 마진 −428%**.
6세트 2건은 **세트수 == 박스내품(6)이라 N=1 기본값이 맞아** 4.0% 정상 — §8-B(4) 규칙대로.

### 조치
`hapo_multiplier.csv` 에 **1세트 리스팅만** N=1/6 추가.
- ESM `6545511386`(무료배송)·`6545511384` → 채널 `esm`
- 캐시노트 `300035`·`300036` → 채널 `cashnote`
- 값은 **정확한 `0.16666666666666666`**. 형제 코드 `51-31`(박스내품 6)이 이미 정확값
  (1/3·1/2·2/3)을 쓰고 있고, `148,500 ÷ 6 = 24,750` 이 원단위로 떨어져 검산이 된다.
  (KB 2026-08-31 메모의 `0.1667` 은 계획값이었고, 스마트스토어 바코드에는 그 값이 들어가 있다 —
  채널별로 4원 차이라 무해하나 표기가 갈린 점은 기록해 둔다.)

### 결과
| 채널 | 리스팅 | 매입가 | 마진율 |
|---|---|---|---|
| ESM 1세트 34,700 / 31,200 | 무료배송 / 별도 | 148,500 → **24,750** | −428% → **4.11% / 4.16%** |
| 캐시노트 1세트 32,200 / 28,900 | 무료배송 / 별도 | 148,500 → **24,750** | −428% → **4.22% / 4.09%** |

ESM 마진미달 95→93건 · 평균마진율 8.56%→9.24%. 기존 행 무변형 + 정확히 2행 추가를 매 커밋 전 대조.

### ★ 전 채널 스윕 (같은 유형 재발 방지)
10개 채널 저장 listing 전건에 `compute_listing` 을 돌려 **선물세트 유니버스(75코드) ∩ 마진율 < −50%**
를 스캔 → **ESM 2건 · 캐시노트 2건, 그 외 8채널 0건.** 둘 다 이번에 해소.
이 스윕은 재현 가능하므로 **신규 등록 배치 직후 루틴으로 돌릴 것**(§8-B 8번의 사후 검증).

★ 조회 실수 1건 — 처음에 `listing_esm.csv` 를 `관리코드` 컬럼으로 걸렀는데 실제 컬럼명은 `코드`라
"50-72 계열 없음"으로 나왔다. 채널 스윕 때도 `CHANNEL_CONFIG` 키(`스마트스토어` 등)로 파일명을
만들어 ESM 하나만 돌았다 — 파일키는 `cfg["key"]`(`smartstore`). **참조 파일은 헤더·키를 먼저 찍고
쓸 것**(2026-08-20 함정의 반복).
