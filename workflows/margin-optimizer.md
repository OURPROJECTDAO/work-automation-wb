# 워크플로우: margin-optimizer (기준마진율 최적화 · 두뇌④)

> 이 워크플로우를 건드리기 전 이 파일을 읽는다. 전역 함정 pitfalls.md, 근거 decisions/0026, 연계 intelligence-layer.md(두뇌③ 탄력성·채널 비교 재사용)·channel-margin-monitor.md(기준마진율 편집·출력).
> ✅ **v0 + 측정 루프 라이브(2026-06-19).** core/intelligence/margin_optimizer.py·decision_log.py + app/pages/13_기준마진율최적화.py(탭: 작업목록/측정 결과). ⑧시즌·⑦매출목표·②회전·cmm prefill 후속. 이 문서가 인계 정본.

## 한눈에 보는 로직 (설명서 · 코드 정본)
권장 기준마진율이 정해지는 순서(한 상품×채널). recommend_code 그대로.

**1) 기준(베이스)** = 채널들을 월순이익 run-rate로 줄세워 누적 85% 채우는 핵심 채널들의 **순이익 가중평균 마진**.

**2) 채널마다 기본 판단(위에서부터 먼저 걸림)**
| 상황 | 액션 | 색 |
|---|---|---|
| 이익 비중<1% & 핵심 아님 | 가격 테스트(2회 무반응 시 보류) | ⚪ |
| 핵심 채널·마진<기준 | 올림(기준까지 절반) | 🟢 |
| 핵심 채널·마진 적정 | 유지 | 🟢 |
| 적게 파는데 마진>기준 | 내림(기준까지 절반) | 🟡 |
| 많이 파는데(≥10%) 마진<기준 | 올림(기준까지 절반) | 🟡 |
| 그 외(적게 파는데 쌈) | 낮게 유지 | 🔴 |

**3) 매출목표 덮어쓰기**(가장 잘 파는 채널도 월 100만 미만 = 작은 상품)
- 이미 마진<기준 → 낮게 유지 🔴(가격 외 요인) / 아니면 → 내림: 나들 마진까지 절반(나들 없으면 −1%p) 🟡

**4) 재고 회전 덮어쓰기**(소진예측 180일 초과)
- 심각도 비례 마크다운(360일=−2%p 캡), 기존보다 더 깊게 내릴 때만. 이미 쌈/바닥 → 🔴 / 아니면 재고정리 내림 🟡(재고 풀리면 자동 원복)

**5) 마무리 보정** — 변동<0.3%p→유지 🟢 · 변동>3%p→🔴 · 변동인데 신호 약함(판매<6개월 or 마진 거의 안 흔듦)→🟡

**🟢🟡🔴**: 🟢=확신 높고 작은 변동(일괄) · 🟡=보통 변동(검토) · 🔴=큰 변동·쌈+안팔림·가격 외 요인·충돌(사람 판단).

**안전장치(항상)**: 절반씩만 · 역마진 금지 · **3% 절대 하한(3%까지만 내리고 그 밑은 막음(clamp) · 이미 3% 이하라 더 못 내리면 작업목록 제외)** · 전부 사람 저장 · 측정 루프가 다음 사이클 검증(악화→되돌림) · 선물세트·나들·미관리 채널 제외.

**상수**: 핵심누적 85%(PROVEN_CUM)·관망 1%(IGNORE_SHARE)·절반 0.5(HALF_STEP)·큰변동 3%p(BIG_MOVE)·미세 0.3%p(MIN_DELTA)·신호 6개월/1.5%p(MIN_MONTHS/FLAT_STD)·매출목표 100만(TARGET_FLOOR)·테스트 1%p(TEST_CAP)·장기소진 180일(TURN_LONG_DAYS)·회전캡 2%p(TURN_CAP_PP)·**절대 하한 3%(MIN_SELL_MARGIN)**. 페이지 상단 expander에도 동일 요약.

## 0. 한 줄
P×C(상품 ~600 × 채널 8 ≈ 5,000)마다 **최적 기준마진율**을 데이터로 잡아 채널마진모니터 편집창에 권장값으로 prefill. 사람이 못 만지는 5천 개를, 우리가 쥔 단 하나의 레버(기준마진율 시트)로 순이익 최대화하도록. 두뇌③(탄력성 서술)을 처방으로 올린 단계 = 두뇌④.

## 1. 목표 / 원칙 (사용자 확정)
- **최대화 대상 = 순이익(원).** "괜찮은 마진 범위"면 매출 볼륨 우선.
- **하한 = 나들커뮤니케이션 마진율.** 가장 공격적인 B2B 마트 채널 = 가격 바닥 기준. 처음부터 거기까지 갈 필요는 없음(이미 볼륨 깔아주는 채널이 있으니).
- **★ 절대 하한 = 3%(MIN_SELL_MARGIN, 2026-06-22 사용자 확정).** 어떤 경로로도(나들·회전 포함) 3% 미만 불가. **3%까진 내리되 그 밑은 3%로 막음(clamp)** — 권장이 3% 미만이어도 인하 자체는 살려 3%까지 내림(인하 액션 유지·노출). **단 이미 3% 이하라 더 못 내리는 셀은 유지로 두고 작업목록 제외**(인하 불가=결정할 게 없음). 나들 마진<3%여도 3%에서 막음(나들 예외 없음). §23.
- **운용 인력 = 현재 무시**(나중 고려). 지금은 매출·순이익 볼륨 우선 작전.
- **채널 독립 = 새 수요로 본다**(사용자 확정 Q3=A). 채널끼리 손님이 달라 → 한 채널 인하가 타 채널을 잠식하지 않음 → **채널별로 따로 최적화 OK**(교차잠식 모델링 불요).
- **순이익 꼭짓점에서 멈춤**(Q4): 순이익 최대 지점이 "가장 좋은 마진". 볼륨 더 가지려 순이익 양보 안 함. ★단 꼭짓점이 현재 마진의 좌/우 어디인지는 상품마다 다름 = 데이터로 풀 문제(ε×마진>1인 동안만 인하가 순이익↑).

## 2. 베이스 (상품 적정 마진) — 척추
- **베이스 = 순이익 누적 ~85% 차지하는 proven 채널들의 *순이익 가중평균* 마진.** (단순평균 아님 — 순이익이 목표라 큰 채널 쪽으로 기움. Q9=가, Q10=가+가중)
- proven 채널 경계 = **순이익 상위 누적 ~85%**(절대 개수/볼륨 임계 아님 — 상품마다 단가·물량 천차만별이라 상대 기준). 검증 15-04: 식봄+캐시노트+스마트 = 순이익 96% → 이 3개, 베이스 ≈ 6.4%.
- proven이 1개뿐이면 → 베이스=그 채널(평균 불요). 게이트 🟡(불안정).

## 3. 채널별 행동 — 볼륨 ×(마진 vs 베이스) 4분면
- **고볼륨(proven)**: 베이스를 *정의*함. 유지.
- **저볼륨·고마진(>베이스)**: 가격이 높아 안 팔리는 가설 → 베이스로 **절반스텝** 끌어내림(노브1). (검증 15-04 알리 11%·배민 8%)
- **저볼륨·저마진(<베이스)**: 이미 싼데도 안 팔림 → 가격 문제 아님 → **hold-low**(낮게 유지, 올리지 않음). 팔리기 시작하면 재평가. (검증 15-04 ESM 4.9%)
- ⑤ 4분면이 *절대 마진*이 아니라 **베이스 대비**로 도는 게 핵심.

## 4. 탄력성 판단 (시장가 대신 채널 내부 비교)
- 같은 상품의 채널 간 (고객가, 볼륨) 편차에서 가설을 빌려옴(⑥). **더 싼 채널이 더 잘 팔리는 게 아니면 가격이 제약이 아님 → 더 안 내림.** (검증 15-04: 스마트 −0.5%(시장가 대비)가 식봄 +1.9%보다 더 팔리는 게 아님 → 가격 인하 불요 → 적정 6.4%.)
- 마진을 **흔든 이력**이 있으면 그 셀의 마진↔볼륨으로 데이터 최적. 없으면(평탄·corr≈0, 예 31-04-02) **시도→측정 루프**(실험)로 발견. 관찰 데이터의 인과 한계를 실험 루프가 메움(= Gate 2·3 작동).

## 5. 노브 4종 (확정)
1. **저볼륨·고마진 끌어내림 = 절반 스텝.** 베이스까지 거리의 1/2씩, 다음 사이클 볼륨 반응 보고 또 절반 / 안 늘면 되돌림. 한 방 인하·외삽 금지(그 채널 탄력성 모름 — 가격 탓인지 수요 부재인지 미지).
2. **회전(②) 인하 = 심각도 비례 + 캡 + 자동복귀.** 트리거=소진예측일 길수록(두뇌② 사장재고/장기). 한 사이클 **−2%p 캡**으로 점진. 재고 풀리면 회전 정상화 → 베이스로 자동 복귀. 바닥=역마진 금지 + margin_floor(제조사 단가하한). ★베이스 아래로도 일시 허용(청산). 단방향(회전 빠르다고 ↑ 안 함).
3. **게이트 = 전부 사람 저장 거침 + 3단계 플래그.** 라이브 자동반영 없음(Gate 1 = 사람은 결정만). 🟢수락(작은 절반스텝·신호 신뢰·충돌 없음, 일괄 처리) / 🟡검토(proven 1개뿐·중간 변동) / 🔴필수(①라벨 충돌 ②|Δ|>3%p ③매출목표·시즌 상품 ④신호 없음=실험 후보).
4. **관망 임계 = 비중 <1%는 *제외* 아니라 *저우선 실험큐*.** 소형 채널도 가격 테스트 1번은 줌(가격이 막았는지 관찰) → **2회 무반응이면 park(쉼)**. park도 영구 아님(분기·원가·시즌·시장 변동 시 자동 재시도). **신규 상품/채널은 항상 실험 대상**(이력 없음 → 관망 불가). ★"관망"은 "작다"가 아니라 "테스트 무반응" 상태(콜드스타트 채널 영구배제 방지).

## 6. 라벨 로스터
**채택 — 자동(데이터가 붙임)**
- ⑤ 마진×볼륨 4분면(베이스 대비) — 뼈대. 신규 상품 빠른 기준 탐색 포함.
- ⑥ 채널 편차 — 탄력성 가설(비싸도 잘 팔리는 채널 = 가격 둔감 → 마진 유지/↑).
- ② 재고 회전 — ✅**구현(2026-06-19)**: 소진예측일>180일(두뇌②) → 마크다운(램프 360일=−2%p캡·단방향·베이스아래 청산·나들/0 바닥·저마진은🔴·자동복귀). 장기소진만 마진↓.

**채택 — 사람(≤600 관리코드에 라벨)**
- ⑦ 상품 매출 목표 — ✅**구현(2026-06-19·자동 도출, csv 입력 불요)**: best 관리채널 월매출 < 100만 = 미달 → 볼륨 푸시(고마진/베이스근처→**나들 마진까지 절반스텝**·없으면 −1%p·저마진→🔴). best≥100만=라벨 없음. 측정 루프 제동. ★원안 'best(나들포함) 따라가기'는 나들=바닥이라 무한인하 → 폐기, 100만 절대 floor만 채택.
- ⑧ 시즌/명절 — ✅**구현**(2026-06-19): `product_attributes.csv 식품음료=='선물세트'`(65종) build_prod에서 제외(작업목록·측정 공용). 명절세트 별도관리·평시 run-rate 왜곡(설13억·추석29억 스파이크) 제거.

**보류**
- ① 시장가(nadl) — ESM 등 쿠폰 왜곡으로 우리 역산 gross ≠ 고객 실결제가. 일단 빼고 sell-through(순마진+볼륨)로 해석. **복귀 선결 = 고객 실결제가↔nadl 비교 정의**(market_nadl 매핑은 이미 있음).
- ④ 재구매율 / ⑨ 미끼(대표상품) — **선결 = 채널×상품 고객 데이터 구조화**(고객키/합포박스키는 적재됨 ADR 0021, 채널×상품 재구매 집계는 미정).

**제외 (라벨 아님 — 대시보드 알림만)**
- ③ 원가추세(수정로그·2C) — 일시적이라 라벨 부적합. 두뇌①·데일리 가격변동알림이 이미 커버.

## 7. 결합 메커니즘
- **(b) 베이스+가감 + (c) 충돌만 추천탭.** 제품 단위 라벨(②⑦ + 베이스)이 가드레일/전반 자세를 깔고, 채널 단위 라벨(⑤⑥)이 채널마다 ±조정. 진짜 충돌(예: 회전 김→내려라 vs 이미 시장 최저→가격 문제 아님)은 자동 말고 🔴 추천탭에 띄워 사람 판단.
- **(a) 단일 서열은 부적합**(실증): 라벨이 서로 다른 그레인(①②⑦=제품 단위 / ⑤⑥=채널 단위)·다른 측정이라 한 줄로 못 세움.

## 8. 출력 / 루프
- 출력 = (관리코드, 채널)별 **권장 기준마진율** + 플래그(🟢🟡🔴) + 이유(베이스·4분면·회전 등).
- → 기존 **channel-margin-monitor 기준마진율 편집 data_editor에 prefill**(채널 baseline_col만 수정·라이브 read 즉시반영, ADR 0016 재사용). 사람 확인 후 저장.
- → 저장 시 listing 가격변경(절반스텝 권장가) 양식 생성(cmm 가격변경 빌더 재사용).
- → 다음 사이클: 1d listing 스냅샷·매출자료로 **반응 측정**(볼륨 변화) → 유지/추가 인하/되돌림. = 시도→측정→학습 루프(Gate 2·3).

## 9. 데이터 (전부 적재됨 — 재적재 0)
- 매출(정산 진실)·순이익·볼륨 = master/sales_YYYY-MM.parquet(매출자료). 마진율=순이익/정산액(기준마진율 시트 정의).
- 실측 택배 = ship_alloc(8 EA 채널 송장 실배분·합포 ceil(팩/3)·00-12 정합). 비-EA 채널(나들 등)은 채널 00-12 총택배 정확·품목 분배는 물류량(수량÷박스내품) 추정.
- 회전 = 두뇌② stockout(현재고÷소진율·입고주기 cadence).
- 베이스/대비 = channel_compare(두뇌③ compute_online_margin·channel_breakdown 재사용).
- 시장가(보류) = market_nadl(nadl_map.csv·prices_*.parquet).
- 기준/하한 = baseline_margin.csv·margin_floor.csv.

## 10. 전용 함정
- **마진 정의 = 기준마진율 시트와 동일**(순이익/정산액, 실택배비 2700/박스). 매출자료 `판매이익`은 택배 전 gross → 반드시 ship_alloc 택배 빼서 순이익으로. (분모를 매입가로 두면(두뇌③ A/B 정의) 값이 달라짐 — 시트 prefill엔 정산액 분모.)
- **비-EA 채널 품목 마진은 추정**(나들 등 ship_alloc 미적용 → 물류량 비례 배분). 채널 총마진은 정확. 박스내품 master 값 정확도 의존(불일치 시 그 품목 택배 왜곡 — 예 코카콜라355 master 박스내품 24 vs 상품명 88).
- **ESM 등 쿠폰 채널 = 우리 역산 gross ≠ 고객 실결제가** → 시장가 비교 왜곡. ESM은 "비싸다"가 아니라 "순마진 5%인데도 안 팔린다"로 해석(sell-through).
- **관찰 데이터 = 상관≠인과.** 과거 실적 서술은 신뢰. "마진 올리면/내리면 ~된다"의 미래·가정은 실험 루프로 확정(행사·계절·품절·성장추세 교란). 성장추세가 corr를 과장(고마진=초기 소물량·저마진=최근 대물량).
- **관망 ≠ 영구배제**(노브4) — 콜드스타트 채널/신규 상품을 영영 못 키우는 함정 방지. 2회 테스트 무반응만 park, 주기 재시도.

## 11. 검증 (설계 단계 — 실데이터 인터뷰)
- 15-04 스팸클래식340(nadl 매칭): 채널 7개 풀 라벨 산출. 순이익 96% 상위 3채널, 베이스≈6.4%, 액션=캐시노트↑ 1칸. ESM(+23.5% gross·물량 64)=쿠폰 왜곡→hold-low.
- 31-04-02 코카콜라500 스마트: 18개월 마진 평탄·corr≈0 → 신호 없음 = 실험 대상(게이트 정상 작동).
- 나들커뮤니케이션: B2B 마트 채널·택배 4.64억/12개월 → 총마진 10.6%→순마진 4.0%(하한 채널의 실체).

## 12. 다음 (미구현)
- ✅ 측정 루프·✅ ⑧ 시즌제외·✅ ⑦ 매출목표+나들floor·✅ ② 회전 markdown 완료 = **설계 노브 전부 구현**. 잔여(나이스투해브): cmm 편집창 직접 prefill(현재 '기준마진율도 함께 변경' 토글이 baseline 직접 write로 갈음) · margin_floor 제조원가 하한 명시 클램프.
- 선결 트랙: 채널×상품 고객 데이터 구조화(④/⑨) · 고객 실결제가↔nadl 정의(① 복귀).

## 15. 측정 루프 (Gate 3 닫기) 구현 (2026-06-19)
결정 기록 → **결정일 이후 실적**으로 효과 측정 → 유지/되돌림. v0가 "기록"까지였다면 이제 "측정→조치" 루프를 닫음. (로그 2026-06-19-margin-optimizer-measure-loop.md)
- **사용자 확정**: 측정창 = **30일**(빠른 피드백) · 되돌림 = **버튼이 baseline Δ 역가산 자동 원복**(승인 후).
- **core margin_optimizer**: `MEASURE_MIN_DAYS=30`·`RESP_BAND=0.10`(±10% 월순이익 밴드)·`_verdict(action,before,after,ready)`·`measure(prod,pending,today,min_days)`.
  - measure = 각 pending의 (관리코드·채널) **ts 이후 거래만** 월 run-rate. ★**커버리지 게이트**: ready = (적재 최신거래일 data_max − ts) ≥ 30일(벽시계 아님 — 익월초 적재 갭에 안 휘둘림). ★**일수 정규화**: 월순/월볼 = 합 ÷ (측정일수/30.4)(달력월 개수 아님 — 월말/부분월 편향 제거). ready+post매출0=월순0(붕괴). 동일 마진정의(순/매출). 신규 컬럼 측정일수.
  - _verdict: before>0면 ±10% 밴드, before≤0면 절대델타(thr=max(|before|×0.1, 1000)). 개선→유지(↓면 "추가 인하 후보")·악화→되돌림·무변화→유지(관찰). 미ready/post0=측정대기.
- **core decision_log**: `update(records,pat,repo)` — decision_id로 기존 행 in-place 갱신(측정/조치 공용). 스키마 무변경(18컬럼; 측정후마진은 화면계산만 미저장).
- **page**: 탭 도입(작업목록/측정 결과). `build_prod()` 공용 캐시 분리·`_apply_baseline` 모듈레벨 승격. 측정 결과 탭 = KPI(측정가능·개선·악화·대기) → ① pending·ready 라이브 표 + [측정 확정](status pending→measured) → ② measured 표 + [유지로 닫기](closed)/[되돌림](baseline −Δ 역가산 원복 + reverted, 원복 payload 미리보기 후). 측정대기 expander.
- **되돌림 = 상대 역가산**(현 타깃 + (마진_before − 마진_적용)). 절대복원 아님(ADR 0027 상대 철학). 기록만 결정(마진_적용==마진_before)은 원복 대상 아님 → 종료만.
- **status 어휘**: pending→measured→closed/reverted (forward·비-PII). 45일 억제는 pending만(measured 이후는 새 결정 위해 재노출).
- **판정 = 제안**(행사·시즌·품절 교란 미보정·관찰 한계) — 사람 최종.
- 검증: 합성 스모크 — 월말결정+완결월=개선·**월말결정+부분월=측정대기(거짓악화 방지·핵심)**·오늘결정(cov음수)=대기·ready+매출0=악화. ast OK · 원장 0행(forward) → 실측은 결정 누적+적재 30일분 후(현 데이터 5월말→6월분 익월초 적재돼야 시작). ★core 변경 → **Reboot 1회 필요**.

## 16. ⑦ 매출목표 + 나들 floor 구현 (2026-06-19, 나)
작은 상품(어디서도 안 큼)을 볼륨 푸시로 키우는 라벨. (로그 2026-06-19-margin-optimizer-sales-target.md)
- **설계 단순화**: 원안 "best 채널(나들 포함) 매출 따라가기"는 나들=바닥 채널이라 모든 채널 무한 인하 → 폐기. **100만원 절대 floor만 채택**(작은 상품·단방향·바닥 고정). (나) = 표시+자동 인하 제안.
- **트리거**: best **관리**채널 월매출 < `TARGET_FLOOR`(100만) = 미달(`목표=📉미달`). best≥100만 → 라벨 없음(베이스 4분면+측정 루프).
- **미달 동작**: 고마진/베이스근처/proven → **나들 마진까지 절반스텝 인하**(거리 절반·역마진 방지). 나들 마진 없으면 → fallback `−TEST_CAP`(−1%p). 이미 저마진(<베이스) → 🔴(가격 외 요인 의심·사람판단). 전부 측정 루프 제동.
- **나들 floor anchor**: nadl_margin = 나들 순마진(_순/판매금액) per 관리코드. page `build_prod`가 **나들 제외 전 1패스**로 nadl_map 계산 → worklist에 주입. 나들 = floor 계산 포함·작업목록 제외(기존). ship_alloc 미적용분(나들 택배=추정)이라 근사 floor.
- core: `recommend_code(cells_code, nadl_margin)`·`worklist(cells, codes, nadl_map)`·상수 TARGET_FLOOR 100만·TEST_CAP .01·출력 `목표` 컬럼. page: build_prod (prod, nadl_map) 반환·목표 컬럼·미달 KPI.
- 검증 합성: small+나들(9→6.5절반)·저마진🔴·없음−1%p·big 미적용. ast OK. ★core 변경 → Reboot.

## 17. ② 회전 markdown 결합 구현 (2026-06-19, 노브2)
재고 안 빠지는(장기소진·과잉) 상품 청산 마크다운. 두뇌④에 재고 신호 결합 = 마지막 노브. (로그 2026-06-19-margin-optimizer-turnover-markdown.md)
- **소스**: 두뇌②(stockout) `소진예측일`=현재고÷최근3개월 일소진(pm+sales·cadence 무관). page `load_turnover`=depletion_rate+forecast(cadence={})→{관리코드:소진예측일}. ⚪사장재고(매출0)는 cells 밖 → 회전 대상=매출 있는데 소진 긴(과잉) 상품.
- **규칙**: 소진예측일>TURN_LONG_DAYS(180) → 램프 마크다운(180→360일=0→−2%p TURN_CAP_PP). 단방향(기존보다 더 깊게 내릴 때만·↑ 안 함)·베이스아래 허용(청산)·바닥 나들/0(역마진 금지)·이미 저마진/바닥은 🔴(충돌·§7). 자동복귀=재고 풀리면 신호 소멸→베이스 복귀.
- core: `recommend_code(...,turnover_days)`·`worklist(...,turnover_map)`·상수 TURN_LONG_DAYS 180·TURN_CAP_PP .02·액션 `↓ 회전`(ACT·MIN_DELTA 포함). page: stockout import·load_turnover·turnover_map 주입·캡션.
- 검증 합성: 360=−2%p·270=−1%p·단기무·저마진🔴·나들없음도 동작. ast OK. ★core → Reboot.


## 18. UI 운영 편의 + 채널 스코프 (2026-06-19, page-only)
- **표 wide 레이아웃**(st.set_page_config layout=wide) — 컬럼 多라 좌우 폭 확장.
- **채널 칩(st.pills selection_mode=multi)** — 멀티셀렉트/체크박스 행 대체. 전부 선택=전체·칩 토글로 제외. 플래그도 칩. (구버전 폴백=멀티셀렉트 hasattr 가드)
- ★**8 관리채널 스코프 한정**(`_MANAGED` = 스마트스토어·ESM·식봄·캐시노트·알리·쿠팡·배민상회·올웨이즈). build_prod가 라벨 후 _MANAGED만 keep → **나들·제이티유통·11번가·셀러허브·리테일앤인사이트·멸치 등 미관리/유통 상호명 제외**(라벨 매핑 없으면 상호명 그대로 떠서 채널 13개로 번지던 것 + 깨진 칩 가독성 해소). **함의**: 베이스/proven 계산도 8 관리채널로 한정(더 정확 — 기준마진율 적용 가능한 채널만·B2B 유통이 base 왜곡 방지). 나들 floor anchor는 여전히 제외 전 1패스로 계산.
- Reboot 불요(page-only). 사용자 요청(2026-06-19 스크린샷 — 채널 13개 칩 깨짐).
- **사유 문구 전체 쉬운 한국어화** + **액션 라벨 평어화·상수화**(A_HOLD 유지·A_UP 올림·A_DOWN 내림·A_HOLDLOW 낮게 유지·A_TURN 재고정리 내림·A_QUEUE 가격 테스트). 연결 로직 상수 참조로 통일(_MOVE_ACTIONS=미세컷·신호약함, _DOWN_ACTIONS=_verdict 추가인하후보, page ACT/실험큐 필터=mo 상수). core 문구/라벨이라 **Reboot 1회**.

## 19. UX — 기록 후 이어서 진행 (2026-06-19, page-only)
- '기록(+기준마진율 변경)' 성공 시 **st.rerun()** → 처리한 행이 작업목록에서 즉시 사라지고 다음 배치로 바로 이어짐. **필터(채널 칩·플래그·검색)는 키 위젯이라 유지**.
- `st.session_state['mo_recent']`(이번 세션 기록한 (관리코드,채널))를 45일 억제(_sup)에 합쳐 **즉시 숨김** — GitHub 원장 복제 지연으로 read_all에 아직 안 떠도 안 보이게.
- 표 key를 `mo_tbl_{mo_tblver}`로 버전화 → 기록 시 +1로 **표 선택 초기화**(stale 선택 방지). 성공 알림은 st.toast(리런 후에도 표시). 에러는 st.error(리런 안 함).
- Reboot 불요(page-only). 사용자 요청.

## 20. 기준마진율 항상 적용 + 백필 (2026-06-19, page-only)
- '기준마진율도 함께 변경' **토글 제거 → 기록 시 항상 baseline Δ가산**(chg=True 고정). 어차피 무조건 함께 변경(사용자). 미리보기 상시·버튼 단일화. baseline 행 없는 낱개/소분류는 자동 '기록만'.
- **백필**: 토글 끄고 기록됐던 미적용분 일괄 적용. 식별=마진_적용==마진_before & Δ≠0. 적용 시 baseline Δ가산 + 원장 마진_적용=권장 동시 갱신(되돌림 일관성). 2026-06-19: 112건 중 106 적용/6 미스(낱개·소분류 baseline 행 없음). (로그 baseline-backfill)

## 21. 작업목록 정리 — 변화없음 분리 + KPI 동적 (2026-06-19, page-only)
- **변화 없음(|Δ|<0.05%p · 권장=현재, 주로 '낮게 유지' 🔴)** → 작업목록서 빼고 별도 expander '가격 외 요인'(노출·상세·광고/단종 판단). 기준마진율 변경 대상 아님이라 필수에 안 띄움.
- **KPI 숫자판(손볼것·🟢·🟡·🔴)을 채널+검색 필터에 동적 반영**(scoped=_apply_chq, 플래그 무관 — 카드 자체가 플래그 분해라). 플래그 칩은 표만 필터. 매출목표 미달 카운트도 scoped.
- Reboot 불요. 사용자 요청.

## 22. 가격 제한(마진 민감) 상품 제외 (2026-06-19, page-only)
- 일부 상품은 가격을 못 움직임(계약가·단가유지·배송비포함 고정가 등) = **채널마진모니터의 '제한 상품'**. 소스 = `reference/margin_floor.csv`(관리코드별 비고='마진율 민감 상품'·제한내용). cmm.compute_listing이 floor의 제한내용/비고→'제한' 텍스트로 쓰는 그 데이터.
- 두뇌④ `load_locked()`로 그 관리코드 set 로드 → 작업목록(act)서 **🔒 별도 묶음(act_lock)**으로 빼냄(억제 직후, Δ분리 전). 작업목록·KPI 제외·baseline 미적용. expander에 제한내용 표시. 채널+검색 필터 따라감.
- 제한 등록/해제는 채널마진모니터 쪽 margin_floor 관리(두뇌④는 읽기만). Reboot 불요. 사용자 지적(가격 못 움직이는 상품 헛추천 방지).

## 23. 3% 절대 하한 — clamp (2026-06-22, 사용자 확정)
- **규칙(clamp)**: 마진 **3%까진 내리되 그 밑은 3%로 막음**. 나들·회전 등 모든 경로 공통 하드 floor(나들 마진<3%여도 3%에서 막음 — 나들 예외 없음). ★사용자 의도 = "3%까진 내리되 그 밑은 막기"(hold+hide 아님).
- **동작**: `recommend_code` 모든 분기(베이스 4분면·⑦ 매출목표·② 회전·hold-low·실험큐) 합류 직후 — `if target < MIN_SELL_MARGIN(0.03)`: ① 현재마진 m>3% → `target=0.03`(3%까지만 인하·**인하 액션/플래그 유지**·사유에 "3% 하한까지만 내림" 부기) → 정상 노출(내림/재고정리 내림 등). ② **m ≤ 3%(이미 3% 이하라 인하 불가) → `A_HOLD, target=m` → 작업목록 제외**(결정할 게 없음, 강제 상향 안 함).
- **후속 자연처리**: 클램프 후 |Δ|<0.3%p(예 m=3.1%→3%)면 기존 MIN_DELTA가 유지로 흡수(미세컷·숨김). |Δ|>3%p면 기존 BIG_MOVE가 🔴(사람 판단). 별도 코드 불요.
- core 상수 `MIN_SELL_MARGIN=0.03`. ⚠️ core 변경 → **Reboot 1회**. 검증 합성 6케이스: 회전 4.5%→3.0 노출·⑦나들 5%→3.0 노출·현재 2% 숨김·m3.1% 미세컷 숨김·8%→4.2 🔴 노출·정상 11%→9% 노출.

## 관련
- decisions/0026-margin-optimizer.md
- logs/2026-06/2026-06-19-margin-optimizer-design.md
- intelligence-layer.md(두뇌③ §6 ③·channel_compare) · channel-margin-monitor.md(기준마진율 편집 ADR 0016) · daily-dashboard.md

## 13. v0 구현 (2026-06-19)
- **상태 = partial(라이브).** ★core 신규 import → **Reboot app 필요**.
- core `margin_optimizer.py`: cell_stats·base_margin·recommend_code·worklist(순수함수). 상수 PROVEN_CUM .85·IGNORE_SHARE .01·HALF_STEP .5·BIG_MOVE .03·MIN_DELTA .003·MIN_MONTHS 6·FLAT_STD .015.
  - proven = **월순이익 run-rate** 누적 85%, **경계 넘는 채널 포함**(prev_cum<.85). 비proven 주력급(≥10%)·베이스미만 → ↑상향 guard. MIN_DELTA 미세 컷.
- core `decision_log.py`: forward-only 원장 → data:`history/decisions.parquet`(18컬럼·seed 완료). read_all·append·init_empty.
- page `13_기준마진율최적화.py`: 두뇌③ 로더 재사용(compute_online_margin·ship_alloc 실측·온라인 그룹). 필터·KPI·작업목록(임팩트순)·행선택→원장기록·CSV. 마진=순이익/정산액.
- 검증: 15-04(식봄·스마트 유지·캐시↑·알리/배민↓·ESM hold-low) 수작업 일치 / 624코드·2596셀 작동.
- v0 한계: 손볼것 53%(임팩트순 top-down)·⑧시즌 미보정(세트류 부풀림)·나들 floor 미클램프(베이스=de-facto)·측정루프/회전/cmm prefill 미구현.
- 다음: 측정 루프(원장 측정후 채움→유지/되돌림) → ⑧시즌 제외 → cmm prefill → ②회전 결합.

## 14. v0 운영 보강 + 마진 정합 (2026-06-19)
페이지/데이터 only — Reboot 불필요. (로그 2026-06-19-margin-optimizer-ops.md · ADR 0027)
- **택배비 2,700 고정**(toggle 폐기) — daily_margin.DEFAULT_FLAT·cmm real_ship과 동일.
- **나들 제외** — 채널명 "나들" 포함 행 작업목록서 제거(데이터 확인용·미관리).
- **45일 억제** — 결정원장(decision_log) read → 최근 45일 pending (관리코드×채널) 숨김. KPI "측정중(45일 숨김)". 같은 결정 반복 노출 방지.
- **UI** — 월매출 컬럼·관리코드 플래그 옆·권장변화 ▲올림(빨강)·▼내림(파랑) 한국식 화살표.
- **기록+기준마진율 변경 통합 토글**(기본 OFF=기록만) — 켜면 미리보기(기준→새기준) 후 baseline_margin.csv 갱신 → cmm 반영.

### 마진 정의 정합 (ADR 0027) — 중요
- cmm 기준마진율 = **노출가(표시가) 기반 forward target**(정산=노출가×(1−수수료)+배송비×계수). 두뇌④ = **매출자료 realized**(정산=실제 정산금액, 쿠폰·할인·믹스 반영). **같은 공식, 다른 정산액.**
- 매입가 ≈ 동일(product_master). 택배: cmm 플랫(2700/박스내품) vs 두뇌④ ship_alloc 실측 → 약간 다름.
- 실데이터(15-04 캐시노트): cmm 타깃 8.56% vs 두뇌④ 실현 5.74% = **실현갭 2.82%p**(대부분 정산 차이).
- ★**권장(realized)을 cmm 타깃에 절대값으로 쓰면 방향 역전**(실현 하락). → **기준마진율 변경 = 현 타깃 + Δ**(Δ=권장−현재). 기준값 없는 행은 기록만. 작은 절반스텝 전제(할인갭 ≈ 일정).
- 표시: 기준마진율(현 타깃) 컬럼(load_baseline) · 채널→baseline 컬럼 매핑 _BCOL(ESM(G마켓·옥션)→ESM).
